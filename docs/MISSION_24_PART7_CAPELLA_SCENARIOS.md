# Mission24 Part7 Slice2 — CAPELLA Scenarios

Ephemeral server-authoritative assumptions, no financial/history persistence or provider request. Existing selected review GET assembly is extracted byte-for-byte into same-context helpers; new capellaScenarios is additive. Calculation-only POST accepts body-bound digest, explicit saved price choice and at most two independent favorable/adverse assumptions. It preserves owner/admin and cookie-CSRF authority, current tenant/demo state and existing private source filtering. No pricing approval, migration or stored preview receipt.

Inputs: signed two-decimal currency changes to aggregate direct costs, incremental overhead and net-before-tax price. Blank stays unknown;0.00 explicitly unchanged. Each active scenario records an owner explanation, optional source/date fields and separate-overhead acknowledgment. Reference publication does not verify hypothetical money; expired/unknown dates stay visible. Complete selected costs plus additional-only overhead produce modeled costs; net price minus modeled costs produces remainder and shortfall. Unknown cost stays unavailable. Zero denominators yield unavailable margin/markup. Existing signed four-place rounding, bounded money and ceiling policy thresholds retained. Policy contingency is separately labeled allowance/budget, never charged twice as actual incurred expense. Minimum-price/target-margin comparisons reuse the saved current-bound policy; tax collected and deposits are excluded from revenue.

Current/original/historical selected financial and decision pins remain untouched. Scenario-basis digest includes current selected price/component/source/policy/commercial/resource references; stale calculation requires refresh. Changes during a pending calculation invalidate that response. No auto-adoption, auto-price or approval transfer. Full commercial approved price uses netBeforeTax; proposed and scope-price-only bases have distinct labels. Historical sources/policies unavailable through current authorized reads remain unavailable.

Shared CAPELLA UI retains separate placement, branding and four-star insignia. Native disclosure, explicit input units, accessible checkbox/labels, field-preserving validation, Clear Analysis and responsive cards. Displayed results are unsaved arithmetic, not profit forecasts or probability/market/safety/availability claims. Full-height test captures temporarily relocate the mounted card while preserving all computed styles, then restore the original tree before actual interactions; they are capture aids, not physical-device or founder visual approval.

Source-controlled capellaScenarioPolicy can disable calculation after authority while retaining all review/history reads. Tests actually exercise populated paid/demo disable and forward; no separately deployed pause build claimed. All71 old SQL files unchanged. No provider, session retrofit, new paid permission, persistent read-side fence or automatic tax integration. Part7 remains3slices: Slice3 connected generative/follow-up integration and open Onboarding Tax Preparation Integration Completion remain separate; Mission25 learning and Part8 quote ownership unchanged.
