# Writer correction-quality ledger

These are pre-freeze writer corrections, not independent audit findings.

- Fixed mounted session fixture header lookup (`headers.Cookie`), then reran
  the authenticated HTTP suite. The initial open handle came from the invalid
  header request construction and disappeared after that fixture correction.
- Added explicit PostgreSQL JSON-type/null checks, finite provenance dates,
  cited specification ordinals, and exact private attachment configuration
  gating. Runtime/provider/browser data cannot approve universal facts.
- Made provider admission read-only, persisted literal identifiers in the
  original immutable draft receipt, avoided repeated clarifications, and
  skipped a second provider call on replay. Reauthorized after the provider wait.
- Added exact existing-asset re-review and new asset-version snapshots,
  preserving private serial data and historical operation pins. Old source
  replays now fail closed even if the current replacement version is reviewed.
- Bound operator to current assignment and new facts to in-progress execution;
  added audit rollback, source fence and distinct concurrent-write tests.
- Corrected actual WebKit focus restoration by focusing the explicit opener
  before native `showModal`; used actual touch activation for touch contexts.
- Prioritized reviewed equipment over legacy aggregate fleet assumptions,
  improved source-link contrast, and isolated independent catalogue controls
  from the general Business Profile dirty-state tracker.
- Corrected browser fixtures to contain the existing profile's Polaris object;
  missing fixture data had triggered a caught legacy profile-render error.
- Updated historical status assertions for the supplied Part 4 release facts,
  and kept pre-asset migration fixtures before additive equipment migration 046.
- Proved broad residual failures on the exact released base. No released
  migration or unrelated provider/financial execution was changed to hide them.
- After staging, the historical tracked-public-script inventory exposed its
  explicit allowlist expectation for the authorized equipment.js addition.
  Updated that expectation and reran verification with all candidate files staged.

Migration 046 is now frozen by `MIGRATION_IDENTITY.md`. Subsequent migration
corrections require an additive file, never replacement bytes.
