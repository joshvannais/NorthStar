'use strict';
const ENTRIES=new Set(['canonical_customer_estimate_version_read','canonical_customer_estimate_version_issue']);
async function grantAndVerify(client,runtimeRole){const role='"'+runtimeRole.replace(/"/g,'""')+'"';const tables=(await client.query("SELECT oid,relname FROM pg_class WHERE relnamespace='public'::regnamespace AND relkind='r' AND relname='canonical_customer_estimate_versions'")).rows;for(const t of tables){const name='public."'+t.relname.replace(/"/g,'""')+'"';await client.query('REVOKE ALL ON TABLE '+name+' FROM PUBLIC, '+role);if((await client.query("SELECT has_table_privilege($1,$2::oid,'SELECT,INSERT,UPDATE,DELETE,TRUNCATE,REFERENCES,TRIGGER') allowed",[runtimeRole,t.oid])).rows[0].allowed)throw new Error('Issued estimate direct ledger authority not withheld');}const functions=(await client.query("SELECT oid,proname,oid::regprocedure::text identity FROM pg_proc WHERE pronamespace='public'::regnamespace AND proname LIKE 'canonical_customer_estimate_version_%'")).rows;for(const f of functions){await client.query('REVOKE ALL ON FUNCTION '+f.identity+' FROM PUBLIC, '+role);if(ENTRIES.has(f.proname))await client.query('GRANT EXECUTE ON FUNCTION '+f.identity+' TO '+role);if((await client.query("SELECT has_function_privilege($1,$2::oid,'EXECUTE') allowed",[runtimeRole,f.oid])).rows[0].allowed!==ENTRIES.has(f.proname))throw new Error('Issued estimate function authority invalid');}if(tables.length!==1||functions.filter(f=>ENTRIES.has(f.proname)).length!==ENTRIES.size)throw new Error('Issued estimate authority unavailable');}
module.exports={grantAndVerify,ENTRIES};
