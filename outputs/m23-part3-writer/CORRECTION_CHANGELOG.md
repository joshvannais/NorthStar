# Mission 23 Part 3 — Writer correction and test change log

| Stage | Finding | Correction | Verification |
| --- | --- | --- | --- |
| Initial migration parse | Migration 039 contained one unmatched closing parenthesis. | Removed the syntax defect before behavior validation. | Normal runner applied 001–039 on disposable PostgreSQL 18.4. |
| PostgreSQL harness start | The local PG18 process restart omitted the suite's explicit UTC server override. | Restarted the disposable loopback cluster with UTC while preserving UTF8/C/checksum evidence. | Identity assertion reports PostgreSQL 18.4, UTC, UTF8, C, checksums on. |
| Historical Part 2 interruption fixture | The Part 2 fixture copied every migration except 038, incorrectly ledgering the new dependent 039 before simulating interrupted 038. | Constrained that historical pre-038 fixture to migration names before 038. Product runtime was unchanged. | Both 038 and 039 interruption/retry suites pass. |
| Database error specificity | Generic stale matching ran before labor category/time-source handling, hiding the precise source-stale result. | Moved labor category/time authority mapping ahead of the generic stale branch and gave category identity its own SQL constraint. | PostgreSQL rejects both as `M23_LABOR_SOURCE_STALE`. |
| Open-timer race | The unique partial index could win before the explicit timer check and originally mapped as generic persistence failure. | Mapped both the explicit and unique-index constraints to the same safe timer-already-open conflict. | Concurrent timers across two executions produce exactly one winner. |
| Cached replay authorization | A generic replay denial obscured dispatch/assignment authority loss. | Kept the response non-oracular but mapped current replay loss to the bounded authority denial used by the route. | Direct assignment revocation and current crew removal both deny replay without effects. |
| Long timer closure | A shared 31-day duration bound unintentionally prevented stopping very old server-owned timers. | Kept the manual/correction bound at 31 days while allowing server timers to close and flagging durations over 16 hours `needs_review`. | Focused rules and PostgreSQL timer tests pass. |
| Correction digest | Correction initially recomputed a manual interval using the action name instead of the retained entry mode. | Preserve the interval's original `manual`/`timer` entry mode through correction. | Corrected interval advances revision with a valid canonical digest. |
| Text boundary | An initial binary regular-expression check was not a robust PostgreSQL UTF-8 control-byte validator. | Reused the explicit bounded UTF-8 byte-loop pattern already accepted in Part 2 while retaining NFC/length limits. | Direct repository-to-SQL control-byte request rejects with zero effects. |
| Performer freshness | Exact assignment pins alone did not prove the performer's membership/account remained active at mutation time. | Reload active workforce-profile, membership, and user authority for the performer within the serializable transaction. | Forged/inactive/out-of-scope cases fail closed. |
| Atomic evidence completeness | Row guards alone did not prove all four immutable companion authorities existed at commit. | Added a deferred completeness trigger matching current state to event, revision, audit, and idempotency evidence. | Forced audit insertion failure rolls the entire evidence set back to zero. |
| Evidence ratification | Historical Part 1/Part 2 assertions still described the now-achieved later-start zero-op as blocked, and one migration-list assertion depended on filesystem order. | Updated only the evolving evidence assertions to the exact PR #162 receipt and sorted the migration list before comparison. | Focused Part 1–3 ratification 22/22; final complete ratification 328/328. |
| Independent audit P1: review overlap | Review was omitted from the mutation overlap condition, so a rejected interval could be accepted after conflicting authority was recorded. | Preserved frozen 039 and added forward-only 040. A review that would produce a non-rejected interval now passes the same worker-wide overlap gate while holding the existing worker serialization lock. | Exact A-reject/B-accept/A-reaccept sequence denies with zero current/history/audit/idempotency effects; concurrent overlapping reviews have one winner and rejected retry remains zero-effect. |
| Independent audit P2: future end | The five-minute future ceiling checked only the client-supplied start instant. | Migration 040 applies the same transaction-time ceiling independently to both manual/correction endpoints; server-owned timer stop remains unchanged. | Manual and correction attempts with near-current starts and far-future ends reject with zero effects. |
| Independent audit P2: source normalization | Part 3 compared transcript source case-sensitively without trimming, allowing variants such as ` Demo `. | Both Part 3 mutation and read functions now use `lower(btrim(transcript.source))` before any effect or cached response. | Whitespace/mixed-case demo and simulation variants deny new mutation, exact replay, and read with zero effects. |
| Terminal independent audit P2: non-space whitespace source bypass | PostgreSQL `btrim(source)` without a character set trims ordinary spaces only; TAB-wrapped `demo` could still reach fresh mutation, exact replay, and read. | Preserved frozen 039/040 and added forward-only 041 with one explicit classifier. It trims ASCII TAB/LF/VT/FF/CR/space and the Unicode White_Space edge set, recognizes only the five canonical ingestion values, and allows labor authority only for `lead`, `retell`, or `voice`. Unknown, embedded-control, and ambiguous values fail closed before replay or read. | Table-driven PostgreSQL cases cover ASCII space, TAB, LF, CR, FF, VT, case, NBSP, Ogham, en quad, em space, line separator, narrow NBSP, and ideographic space across fresh mutation, exact replay, and read; every denial preserves complete current/event/revision/audit/idempotency snapshots. |
| Terminal regression evidence helper | The new zero-effect snapshot query initially treated revisions as if they carried `execution_id` and idempotency rows as if they carried `id`. | Joined revisions through their tenant-bound interval and ordered idempotency evidence by its real key hash. Product SQL was unchanged. | Focused PostgreSQL suite passes all source-policy and zero-effect cases. |
| Audit-regression fixture collision | The two normalized-source cases initially recorded identical worker intervals in separate executions, correctly triggering the strengthened overlap gate. | Gave the two independent source cases non-overlapping fixture instants. Product SQL was unchanged. | Focused PostgreSQL correction suite passes 27/27. |
| Cross-regression execution timing | Running the long account-capability PostgreSQL suite after five other regression suites caused one pre-existing 300-second test timeout although its assertions and every other suite remained green. | Kept product and test source unchanged, preserved the failed timing result, and reran that exact account suite alone against the same PG18.4 UTC authority. | The combined attempt passed 77/78 with only the timeout; the isolated retry passed 11/11, while the other five suites passed 67/67. |

No terminal correction changes protected migrations 001–040. Migration 039 remains
frozen at blob `db96ba632aecea501fd9c1bda3c3dfebf139cad0`, 56,232 bytes,
SHA-256 `2204695c9be757a66094897f6bb9e86bee9c84f1582a3bc27812d7bbdebdf13a`.
Forward-only migration 040 is frozen separately in `MIGRATION_040_IDENTITY.md`.
Forward-only migration 041 is frozen separately in `MIGRATION_041_IDENTITY.md`.
