'use strict';
// No real jurisdiction/source rights or coverage have been approved in this package.
// Reviewed public entries are version-controlled; runtime acquisition cannot publish rules.
module.exports = Object.freeze({ version: 'tax-source-registry-v1', entries: Object.freeze([]) });
