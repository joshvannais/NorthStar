# Part7 Slice3 accounting correction

This correction preserves the inactive release's operational boundary. No provider has been activated or called by its tests. Both connected-call/generative and onboarding-tax checkpoints remain open. Part7 is incomplete.

## Request and cost behavior

The shared runtime constructs and freezes the complete request, including response schema and caller instruction suffix. The full serialized body must fit16,000 UTF-8 bytes; this is an input-size defense, not a token estimate. The new counted path binds model/input/instructions/reasoning/text/truncation to one count followed by one generation, rechecking current source/authority after count. Installed official OpenAI SDK7.8.0 supports those count parameters. Generation-only output/cache/store/safety fields are not sent as unsupported count parameters. Both operations share one20-second abort boundary within the existing25-second outer lease. No DB transaction spans transport.

The production factory hard-blocks grounded/caller generation while count pricing and admission are unresolved, even if v2 flags are supplied. It never wires the count adapter. Internal tests explicitly inject an in-memory count transport. This is not a production configuration switch.

Per root's explicit compatibility direction, existing v1 and equipment extraction retain their existing production availability and do not acquire a new unpriced count call. They use the corrected whole-body byte defense, single generation attempt, cache arithmetic and conservative uncertainty settlement. Their exact input-token envelope remains unproved; this is a retained activation/accounting limitation, not a claim that bytes prove token cost. Changing that compatibility path needs a separately reviewed count admission contract.

One reservation never triggers an automatic second generation attempt. Known explicit rejection and existing durable retry ownership remain separate. Network/unknown5xx/abort outcomes retain the full20,000,000 nanoUSD reservation rather than zero. Exact historical replay is still owned by existing request ledgers/registries and does not make another external request. Cache buckets are validated integers with read+write<=input; cost uses ordinary200/read20/write250/output1200 nanoUSD per token. Invalid usage cannot settle success or release unknown cost. An aborted late response is not delivered; valid known usage can still be reconciled as failed delivery.

## Count admission addendum still required before any activation

The current fixed reservation is20,000,000 nanoUSD. A proved16,000-input/8192-output generation using worst supported cache-write pricing consumes at most13,830,400, leaving6,169,600 nanoUSD ($0.0061696). No supported count fee or entitlement bound is established. Therefore no complete count+generation ceiling is established and no count network path is enabled.

Before activation, establish a pinned count tariff/maximum C (including failed count requests), exact account/model entitlement and request identity. If C exceeds that headroom, the released fixed reservation cannot safely cover the operation. Even when it fits, existing one-provider-ID/usage receipt contracts must explicitly account for count identity/outcome plus generation identity/outcome and their combined charge; count failure and generation uncertainty cannot refund that charge. Present an exact compatibility/schema addendum if those receipts cannot represent the required evidence. No released SQL or fixed cap is widened here. Do not assume the count API is free or SDK support proves billing.

## Source and wording evidence

Official sources: [counting tokens](https://developers.openai.com/api/docs/guides/token-counting), [Luna pricing](https://developers.openai.com/api/docs/models/gpt-5.6-luna), [cache accounting](https://developers.openai.com/api/docs/guides/prompt-caching), inspected2026-09-13. The complete count method web page timed out; installed official SDK declarations and mocked SDK fetch verify supported field projection and endpoint. No live protocol/billing proof is implied.

No HTML, CSS, browser renderer, label, loading indicator or accessible-name code changed. Existing message/error rendering receives the same plain-language errors, plus the gated503: “Conversation is temporarily unavailable. Your saved records remain available.” Mounted demo tests assert that response. Paid401/503 and demo503 projections, current-source suppression, and caller provisional/replay/unavailable states are exercised locally. No new numeric/financial UI or provider-ready claim. Browser theme/layout evidence is inherited, not rerun or newly claimed; independent wording review remains required.

No migrations/startup/SQL grants/policy flags changed. Populated disposable fixture tests cover paid/demo/caller accounting and replay; their ordinary setup applies existing migrations, not a new migration or recovery rehearsal. Previous PR218 populated recovery evidence stays attributed to its executed89b source; final-aa45 pauses were prepared only. This candidate does not relabel those executions.

Queued next after this candidate freezes: M24_POLARIS_VISUAL_REFINEMENT_DISPOSITION.md, eight founder presentation corrections and authentic chat-card light/dark theme reuse across inventoried Polaris surfaces. It does not depend on provider/tax context and is not an additional slice. It is not implemented in this accounting patch.
