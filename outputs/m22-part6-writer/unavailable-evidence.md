# Mission 22 Part 6 unavailable and unclaimed evidence

- `tests/integration/account-migration-010-postgres.test.js`: 24 cases remain
  unavailable because the four disposable `ACCOUNT_MIGRATION_*` PostgreSQL URLs
  were not supplied. They are excluded from the locally available corpus and
  are never called passing.
- Optional Codex security scanning remains unavailable because its configuration
  is unset and the prior ancillary scan failed. It was not invoked, configured,
  or used as an approval gate. Manual complete-diff/source-to-sink and mounted
  adversarial validation continue without erasing progress.
- Hosted GitHub checks/workflows may remain absent; absence is unavailable, not
  passing.
- Physical Safari and physical mobile-device testing are unavailable. Actual
  Playwright WebKit is labeled WebKit, never physical Safari.
- Provider accounts, credentials, configuration, live provider calls, route
  accuracy, production data, production migration, Railway deployment/restart,
  and production authenticated acceptance are not authorized in the writer
  lane and remain unclaimed.
- Legal/recording/AI-identity decisions and user visual approval remain separate
  and unclaimed.
- The in-repository employee-only screenshot package is authoritative writer
  evidence. Copying it to the existing verified canonical OneDrive evidence/
  screenshots path and surfacing key views in the master chat is deferred to
  the Mission 22 lead after terminal Part 6 release; this writer does not write
  OneDrive or invent a destination.
