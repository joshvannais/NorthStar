# Mission24 Part1 Slice4 — Capella recorded-cost comparison

Scope: a private read-only comparison in the existing paid/demo customer drawer. Part1 maps to canonical Phase1, Estimate Architecture; reporting Slice4 does not complete Part1, the full Risk Lens, scenarios, chat requests or customer-safe quote composition.

The current owner/admin estimate-review transaction reads the exact persisted graph and human decision. The Capella projector matches all decision source pins and currency, then compares the saved approved before-tax price to the snapshot's recorded direct-cost aggregate. It never uses machine gross/net profit, sums display components, recalculates pricing, reads a current profile into an old estimate, writes an assessment or grants customer access. Absent/withdrawn decisions, mismatched sources/currency and missing/invalid amounts yield unavailable assessment. Zero is valid.

Amounts use integer cents from exact normalized human-price strings and bounded recorded decimal numbers: up to twelve whole digits and two decimal places, no negative costs, coercion, rounding extra precision or unsafe amount conversion. The signed remainder is limited to recorded direct costs; it is not complete profit, break-even, statistical risk or a margin-floor determination. Vehicle/fuel/other expenses and scenario policies are not invented.

The response carries source pins, decision identity/revision/digest, date, currency, projection version and synthetic status internally. The shared renderer verifies its review association and shows plain business text only. Refresh/loading/error/customer switch removes prior amounts; existing review generation checks and consent invalidation remain controlling. Same demo route/projector/renderer, isolated token-owned decisions; new/reset workspaces include one clearly fictional cost example from the shared canonical calculator, while other jobs retain unavailable cost states. Existing saved graphs and decisions are not recalculated. No private endpoint is called from demo.

No schema, SQL, existing ledger, price engine, provider or runtime grant changes. The source-controlled paused build preserves the comparison and saved decisions while rejecting decision mutations; this is application pause, not physical restore or all-write fencing. The accepted057 release risk disposition is not a future waiver.

## Acceptance and wording inventory

- Deterministic unit boundary cases: exact cents, aggregate versus components/machine profit, zero/shortfall, invalid/absent amounts, currency/source mismatch, withdrawn approval and shared synthetic projection.
- Mounted disposable PostgreSQL: current owner/admin and tenant boundaries, revoked membership, exact selection, second administrator revision, read immutability and existing migration identities.
- Actual Chrome/WebKit shared paid/demo flow: desktop/mobile light/dark, approval/shortfall/withdrawal, missing demo costs, loading/error/denied/mismatched/stale-customer states, keyboard/focus and no clipping. Error interception is presentation evidence, not proof of a live failed dependency.
- Existing focused consent/refresh/cancel/save regression and actual source-patched paused read under both engines. No new migration rehearsal is needed for unchanged SQL; prior migration/physical recovery evidence retains its own identity and limits.

Changed rendered surface: Capella section inside the existing estimate review below contextual Polaris advice/decision. Heading, amount labels, expense limitation, review-required/unavailable/shortfall text and accessibility name are covered. Shared review loading/empty/denied/error/focus states are affected and checked. No new worker/dispatcher/customer view is authorized. Source transport diagnostics never appear as user messages. Restrained gold/amber complements the unchanged traditional purple Polaris; shortfall is explicit text, not color alone. Founder personal visual approval remains separate.

Release evidence belongs to the final sealed packet, not this prospective acceptance list. Preserve previous3 focused API/15 wider failures, unavailable hostedCI, physicalSafari/devices/manualAT/provider/private evidence, physicalrestore/RPO/RTO and later automatic backup coverage. No full Mission24 completion claim.


## Ordinary demo parity correction

The initial Slice4 packet e890794 could only show unavailable demo costs and was independently marked correction-required. Its sealed evidence remains unchanged. The corrected new/reset first existing job stores calculateCanonicalPolaris output from an explicit fictional Business Profile, scope, crew/labor/material/equipment/travel inputs and profile authority. The canonical input and profile hash/version/id are retained with that graph, and its read projection uses those actual calculation pins. No read-time recalculation or second cost total is introduced. Human price approval is still a separate ordinary demo action; revising it can show a positive remainder or shortfall. At least the other existing jobs retain missing-cost practice states.

Fictional input rates are illustrative, not researched market data or a real tenant profile. No provider routing/cost evidence is claimed. Existing stored demo graphs keep their old snapshots, source pins and history; users can use the existing Reset Demo action to create the new example, with the existing reset consequences. The seeded workspace validator's snapshot and graph digests remain intact. The constructor affects only new/reset first jobs, not simulated leads or paid graph ingestion.
