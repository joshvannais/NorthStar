'use strict';
const fs=require('node:fs'),path=require('node:path'),read=v=>fs.readFileSync(path.join(__dirname,'../..',v),'utf8');
describe('Mission 25 Part 11 Slice E material cost observations',()=>{const migration=read('migrations/109_canonical_imported_material_cost_observations.sql');
 test('pins exact plan, source, reviewed links, bindings and imported records',()=>{for(const value of['canonical_external_material_cost_observations','materialPlan','sourceConsent','jobMatch','referenceMatches','bindings','importedRecords','canonical_imported_material_cost_match'])expect(migration).toContain(value);});
 test('keeps all four dimensions independent and preserves explicit boundaries',()=>{for(const value of["'unitCost'","'purchasing'","'vendor'","'recordedBalance'",'currentAvailabilityConfirmed',"'line_total'",'Currency, valuation and unit conversions are not inferred'])expect(migration).toContain(value);expect(migration).not.toMatch(/ILIKE|levenshtein|similarity|exchange.rate/i);});
 test('mounts only guarded backend entry points',()=>{const routes=read('src/routes/learning.js'),db=read('src/db.js');expect(routes).toContain('imported-material-cost-observations');for(const value of['REVOKE ALL PRIVILEGES ON TABLE public.canonical_external_material_cost_consents','REVOKE ALL ON FUNCTION public.canonical_imported_material_cost_basis','GRANT EXECUTE ON FUNCTION public.canonical_imported_material_cost_read'])expect(db).toContain(value);expect(read('public/js/learning-center-page.js')).not.toContain('imported-material-cost-observations');});
});
