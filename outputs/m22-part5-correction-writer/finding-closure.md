# Validated finding closure

## M22-P5-001 P1

One shared current-operator gate now runs before query/response construction on
canonical graphs, dashboard/analytics, surfaces, compatibility projections,
legacy collection aliases, and broad detail aliases. Mounted tests deny ten
forged employee aliases with 403 and prove hostile customer/contact bytes are
absent. Inactive, cross-tenant, and employee/viewer compatibility fixtures no
longer impersonate broad operators. Invalid query values still return 400
before any authority/data query.

## M22-P5-002 P1

The server scans stable keyset pages inside one bounded repeatable-read
transaction, computes complete totals/categories up to the explicit 1,000-row
resource bound, and returns at most 100 rows per page with `shown`, `total`,
`truncated`, current/next cursor, and previous/next truth. Tests cover empty,
100/101+, tenant isolation, stable ordering, page mutation, category totals,
invalid cursors, and exact PostgreSQL microsecond ordering.

## M22-P5-003 P1

Every Command Center schedule/date label, chart bucket, accessible label, and
zone suffix uses the server-provided tenant IANA zone. Chrome and WebKit run in
Los Angeles while the tenant is New York and agree with Calendar, including
DST-aware contract paths.

## M22-P5-004 P2

Every overview item, scheduled or unscheduled, reloads current assignment state
with a shared lock in the same repeatable-read transaction. Graph/appointment
identities are checked and categories/digest derive only from that current row.
The mounted stale revision-1/unassigned reproduction returns the durable
revision-2/assigned authority.

## M22-P5-005 P2

`canRead` now reflects current active owner/admin/dispatcher identity only;
`canMutate` separately includes onboarding and existing subscription policy.
Past-due/read-only operators receive safe Calendar and Command Center truth with
disabled mutation controls while Part 4 preview/apply remains server-denied.
Employees and cross-tenant actors remain denied.

## M22-P5-006 P2

Visible drag/move carries the exact elapsed milliseconds into the shared dialog;
the end is derived from proposed start using the tenant scheduling-time
contract. Resize continues to change end only. Real Chrome and actual WebKit
drag paths create valid previews and durable approvals without pre-approval
mutation. Keyboard and touch alternatives use the same code path.

## M22-P5-007 P2

Part 4 approval responses must contain the exact durable revision/digest. The
dialog closes only after Calendar or Command Center reload proves that exact
pair. Offline/401/5xx/aborted or divergent refresh clears/marks the view stale
and retains a non-dismissible applied-but-refresh-failed dialog with approval
disabled, reason/proposal/evidence retained, and only retry-refresh or reload.
Retry never reapplies the approval.
