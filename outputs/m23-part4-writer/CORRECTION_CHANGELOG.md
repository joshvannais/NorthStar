# Mission 23 Part 4 — Writer correction and test change log

| Stage | Finding | Correction | Verification |
| --- | --- | --- | --- |
| Initial runtime privilege verification | The startup verifier's protected-table list did not yet exclude the five new Part 4 tables from the generic runtime DML expectation. | Added the tables to the protected exclusion list and introduced exact material table/helper withholding plus entry-point EXECUTE assertions. | Real PostgreSQL startup now verifies the intended EXECUTE-only boundary. |
| Unit contract identity | The first draft carried one mistyped character in the material-unit digest at one call site. | Replaced it with the single exact `m23-material-unit-v1` SHA-256 identity used by JavaScript, SQL constraints, mutation, and read projection. | Unit and PostgreSQL identity tests pass. |
| Current row lineage guard | The first reversal guard proved only its source ID and material attributes, not exact execution, assignment, and performer identity. | Strengthened the row guard so a reversal must exactly match the original tenant, execution, assignment, performer, and all material dimensions. | Reversal and forged-lineage PostgreSQL coverage passes. |
| Reversal review lifecycle | A compensating reversal began `needs_review` but the first current-row guard permitted review only for original records; a permanently pending reversal could still affect the balance. Reversing a rejected original would also create an unmatched positive effect because rejected evidence is excluded. | Permit owner/admin review of an exact reversal while preserving its entry kind and source link; forbid correcting or reversing a reversal and forbid reversing an already rejected original. | PostgreSQL proves immutable two-revision review history for a reversal, exact zero balance after acceptance, and zero-effect denial for a rejected original. |
| Response traceability | The first mutation response omitted the server correlation ID stored in immutable event/audit evidence. | Added the server-derived request correlation ID to the stored and returned response. | Fresh response and exact replay remain byte-equivalent in focused tests. |
| Read-current authority | The initial read path rechecked actor scope but did not independently reject a revoked dispatch or completed current appointment. | Joined the current canonical appointment and applied the same current assignment/dispatch/status gates before any material disclosure. | Revoked-dispatch and completed-appointment read/mutation tests deny with zero evidence effects. |
| Mission 22 mission-wide regression | A predecessor exact-runner assertion correctly compared the complete ledger to current source, then separately required migration 037 to remain the final filename forever. Additive Mission 23 migrations made that historical final-position assertion stale. | Preserved the full exact source/ledger equality and restart-zero-op checks, but changed the Mission 22-specific assertion to require its exact 037 row without forbidding additive later migrations. | The predecessor PostgreSQL segment is rerun after this test-only correction. |
| Part 3 roadmap status | The canonical roadmap and evolving Part 3 evidence ledger still described the later-start receipt as pending after PR #164 had released it. | Added the immutable PR/merge/deployment/ledger receipt and changed only current status assertions; historical first-start documents remain historical. | Part 1–3 ratification now proves the achieved gate without rewriting the original receipt timeline. |
| First independent Part 4 audit — nullable pins | SQL three-valued comparisons allowed explicit `NULL` execution, assignment, or existing-movement revision/digest pins to pass validation, including replay. | Forward-only migration 043 wraps the frozen 042 entry point and rejects every required `NULL` pin before authority lock, replay lookup, or evidence access. | Unit and authentic PostgreSQL table-driven mutation/correction/replay probes assert denial and unchanged current/event/revision/audit/idempotency counts. |
| First independent Part 4 audit — revocation races | Material checks could race mutable transcript, appointment, subscription, account, membership, workforce, crew, assignment, or execution authority. | Added one ordered advisory protocol: repository sessions acquire the shared lock before opening the serializable/repeatable-read snapshot; the SQL entries verify that pre-transaction session lock and deny callers that bypass the protocol; every supporting table has a `BEFORE STATEMENT` exclusive-lock trigger. All checks and replay disclosure occur after the lock. | PostgreSQL proves a prior membership revocation blocks mutation until commit then denies, a prior transcript revocation blocks a read until commit then returns 404, direct entry without the pre-transaction lock denies without effects, all 11 authority relations carry the writer trigger, and denials add no evidence. |
| First independent Part 4 audit — Unicode mismatch | The JavaScript validator iterated code points while migration 042 rejected raw UTF-8 continuation bytes, incorrectly denying ordinary international text and accepting several invisible/object controls. | Added one versioned JSON code-point contract consumed by JavaScript and mirrored by migration 043 with its exact source SHA. The contract accepts bounded NFC international text and rejects the enumerated control/invisible/bidi/object/interlinear/tag ranges. | One table-driven corpus is exercised by unit JavaScript and PostgreSQL, including Latin, Han, Arabic, currency, emoji, NBSP, decomposed text, controls, soft hyphen, bidi, object/interlinear, and tag characters. |
| First independent Part 4 audit — balance window truth | The read capped balance groups at 200 but exposed only movement truncation, so callers could mistake a partial balance list for a complete one. | Added strict `balanceOffset`/`balanceLimit` validation and independent `totalBalanceCount`, `balanceTruncated`, and exact previous/next page evidence. The balance aggregate still derives from all visible movements, not the movement response window. | PostgreSQL creates 101 movements producing 202 balance groups, proves movement truncation false while balance truncation is true, and reaches the final two groups through the next bounded page. |
| First independent Part 4 audit — legacy fixture | The M20 workforce upgrade fixture copied later migrations while deliberately omitting their 015/020/032–035 dependencies, a state no released sequential runner could create. | The fixture now builds the complete real historical 001–014 prefix, then upgrades through the unchanged current source set. | The isolated M20 PostgreSQL upgrade/zero-op suite passes without weakening migration or runtime invariants. |
| Daybreak exact-head re-audit — stale MVCC snapshot | The advisory read lock proved current lock possession but did not prove that lock acquisition preceded a caller's already-fixed repeatable-read snapshot. A caller could fix active membership, wait for an exclusive revocation to commit, acquire the shared lock, and read through the stale snapshot. | Forward-only migration 044 makes every existing supporting-authority writer advance a singleton MVCC fence while holding the exclusive lock. Material entries require the shared lock, reject same-backend exclusive reentrancy, then lock the fence row; PostgreSQL rejects a snapshot that predates a committed fence replacement. Runtime has no direct fence privilege. The material repository read is semantically read-only but uses a read-write repeatable-read transaction solely because PostgreSQL forbids row locks in a read-only transaction. | Authentic two-connection PostgreSQL tests cover all 11 authority-writer trigger paths, stale mutation/replay/read, the exact runtime `REPEATABLE READ READ ONLY` disclosure reproduction, direct SQL, no-lock and same-backend denial, zero evidence effects, contention timeout and clean retry, bounded deadlock retry, runtime privilege denial, and migration 044 interruption/retry/restart zero-op. |
| Full-ratification child lifecycle | The historical Mission 19 mounted-entrypoint proof completed its route assertions under Windows Node but exceeded its 15-second child bound before process exit while imported runtime handles remained. | Flush the exact proof result, terminate the completed child explicitly, and extend the outer bounded timeout to 60 seconds for the slower mounted probe. No production source or assertion changed. | The isolated probe passes 9/9 and the complete ratification suite passes 363/363. |

No correction changes migrations 001–043. Migrations 042 and 043 retain their
frozen blobs, byte counts, and SHA-256 identities. Forward-only migration 044 is
frozen separately in `MIGRATION_044_IDENTITY.md`. The historical bounded
production-history receipt predates migrations 043 and 044; a new compatibility
receipt for the exact final source set is required before release.
