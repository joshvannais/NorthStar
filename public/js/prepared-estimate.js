(function () {
  'use strict';
  var groups = [['materials', 'Materials', 'cdMaterialReview'], ['labor', 'Work And Crew', 'cdLaborPlan'], ['equipment', 'Equipment And Tools', 'cdEquipmentPlan'], ['travel', 'Vehicles, Travel And Logistics', 'cdTravelPlan'], ['pricing', 'Price', 'cdPricingPlan']];
  function node(tag, text, parent, className) { var e = document.createElement(tag); if (text != null) e.textContent = text; if (className) e.className = className; if (parent) parent.appendChild(e); return e; }
  function copy(v) { return JSON.parse(JSON.stringify(v)); }
  function dispose(state) { if (state) { state.sequence++; clearTimeout(state.timer); } }
  function adoptionPanel(review, parent, state, options, current) {
    var a=state.adoption||{selection:{},sequence:0};state.adoption=a;a.sequence++;a.busy=false;a.value=null;a.body=null;a.historyLoaded=false;
    var box=node('details',null,parent,'prepared-inputs');box.id='cdPreparedAdoption';var summary=node('summary','Review And Save',box);summary.className='prepared-review-trigger';
    var introduction=node('p','Keep the original estimate and review a new proposed price. Approval and sending remain separate.',box,'prepared-muted');
    var message=node('p','Prepare the current draft before combining plans.',box,'prepared-status');message.id='cdPreparedAdoptionStatus';message.setAttribute('role','status');message.tabIndex=-1;
    var result=node('div',null,box);result.id='cdPreparedAdoptionResult';
    var decisionArea=node('div',null,box,'prepared-save-decision');
    var settings=node('details',null,box,'prepared-review-settings');settings.open=!review.simulated;var settingsSummary=node('summary',review.simulated?'Calculation Details':'Plan Choices And Cost Review',settings);
    var form=node('fieldset',null,settings);node('legend','Plans To Include',form);var fields=node('div',null,form,'prepared-fields');
    function select(label,id,values){var wrap=node('div',null,fields),l=node('label',label,wrap);l.htmlFor=id;var e=node('select',null,wrap);e.id=id;values.forEach(function(v){var o=node('option',v[1],e);o.value=v[0];});return e;}
    var choices={};groups.forEach(function(g){var e=select(g[1],'cdPreparedKeep-'+g[0],[['replace','Use the prepared plan'],['retain',g[0]==='pricing'?'Keep prior pricing unchanged':'Keep costs already in the estimate']]);choices[g[0]]=e;var saved={materials:review.adoptedMaterialPlan,labor:review.adoptedLaborPlan,equipment:review.adoptedEquipmentCostPlan,travel:review.adoptedTravelPlan,pricing:review.pricingPlans?.current}[g[0]];e.value=a.selection[g[0]]||(saved?'retain':'replace');e.onchange=function(){a.selection[g[0]]=e.value;a.costCoverage=null;a.coverageOptions=null;coverageFields(null);changed();};});
    var costLabel=node('label',null,form,'prepared-ack'),costCheck=node('input',null,costLabel);costCheck.type='checkbox';costCheck.id='cdPreparedCostsSeparate';node('span','I reviewed where these costs are included; no amount is counted twice.',costLabel);
    var coverageBox=node('details',null,form);node('summary','Costs Already Included Elsewhere',coverageBox);var coverageBody=node('div',null,coverageBox);coverageBody.id='cdPreparedCostAllocations';
    var overhead=select('Overhead Already Included','cdPreparedOverhead',[['unknown','Needs review'],['disjoint','None of this overhead is included'],['allocated','Part is included in work costs']]);
    var allocations=node('div',null,form,'prepared-fields');allocations.id='cdPreparedOverheadShares';
    var policyLabel=node('label',null,form,'prepared-ack'),policyCheck=node('input',null,policyLabel);policyCheck.type='checkbox';policyCheck.id='cdPreparedPolicy';node('span','Include the current pricing policy, recalculated for these costs.',policyLabel);policyLabel.hidden=!review.pricingPolicies||!review.pricingPolicies.current||review.pricingPolicies.current.sourceUnavailable||!review.pricingPolicies.current.inputs;
    var controls=node('div',null,form,'prepared-actions');function button(text,id,fn,host){var b=node('button',text,host||controls,'btn btn-secondary');b.type='button';b.id=id;b.onclick=fn;return b;}
    var calculate=button('Update Estimate Breakdown','cdPreparedAdoptionCalculate',preview);calculate.className='btn btn-primary';
    button('Cancel Review','cdPreparedAdoptionCancel',function(){changed();box.open=false;summary.focus();});
    if(review.simulated)box.insertBefore(controls,settings);
    var consentLabel=node('label',null,decisionArea,'prepared-ack'),consent=node('input',null,consentLabel);consent.type='checkbox';consent.id='cdPreparedAdoptionConfirm';node('span','I reviewed these plans, sources, included costs and proposed price. Save this estimate for separate price approval.',consentLabel);
    var save=button('Save Prepared Estimate','cdPreparedAdoptionSave',function(){if(!consent.checked||!a.value||a.value.state!=='ready'||!a.body)return;var attempt={key:crypto.randomUUID(),demoRevision:review.simulated?review.demoWorkspaceRevision:null,body:Object.assign({},a.body,{expectedReviewDigest:a.value.reviewDigest,confirmed:true})};a.pending=attempt;persist();send(attempt);},decisionArea);save.className='btn btn-primary';
    var retry=button('Retry Same Save','cdPreparedAdoptionRetry',function(){if(a.pending)send(a.pending);},decisionArea);
    var history=node('details',null,box);node('summary','Prepared Estimate History',history);var historyBody=node('div',null,history);
    var owner;try{var sessionOwner=sessionStorage.getItem('northstarSessionOwner'),sessionId=sessionStorage.getItem('northstarSessionId');owner=sessionOwner&&sessionId?sessionOwner+':'+sessionId:null;}catch(_){}
    var storage=owner?'northstarPreparedAttempt:'+owner+':'+review.pins.estimateId:null;
    try{if(!a.pending&&storage)a.pending=JSON.parse(sessionStorage.getItem(storage)||'null');if(a.pending&&(!a.pending.key||a.pending.body?.version!=='estimate-proposal-adoption-v1'))a.pending=null;}catch(_){a.pending=null;}
    function persist(){if(storage)try{if(a.pending)sessionStorage.setItem(storage,JSON.stringify(a.pending));else sessionStorage.removeItem(storage);}catch(_){}}
    function eligible(){return current()&&review.isCurrent!==false&&state.data&&!state.stale&&!state.dirty&&state.data.readiness!=='blocked';}
    function update(){var overview=document.getElementById('cdPreparedOverview');if(overview)overview.hidden=!!a.value&&box.open;decisionArea.hidden=!a.value&&!a.pending;form.disabled=a.busy||!!a.pending;calculate.disabled=!eligible()||!a.historyLoaded||a.busy||!!a.pending;consent.disabled=!a.value||a.value.state!=='ready'||a.busy||!!a.pending;save.disabled=consent.disabled||!consent.checked;retry.hidden=!a.pending;retry.disabled=a.busy;if(!eligible()&&a.value)changed();}
    function changed(){introduction.hidden=false;a.sequence++;a.busy=false;a.value=null;a.body=null;consent.checked=false;result.replaceChildren();message.textContent='Review the changes and calculate again.';if(!eligible())message.textContent='Refresh and prepare the current draft before combining plans.';update();}
    function row(label,value,parent){var r=node('div',null,parent||result,'prepared-row');node('span',label,r);node('strong',value==null?'Not recorded':options.money(value,review.currency),r);return r;}
    function coverageFields(info){
      coverageBody.replaceChildren();if(!info||info.unavailable){node('p','Review totals once to load the exact work, equipment and travel costs available for allocation.',coverageBody,'prepared-muted');return;}
      if(!a.costCoverage)a.costCoverage={overlaps:[],equipmentOutside:info.equipmentOutside.map(function(x){return{equipmentLineId:x.equipmentLineId,kind:x.kind,component:x.kind==='operator'?'labor':'travel',targetLineId:'',targetCategory:x.kind==='operator'?'travel_labor':'',amount:x.amount,reason:''};}),travelOutside:info.travelOutside.map(function(x){return{travelLineId:x.travelLineId,component:'equipment',sourceLineId:'',amount:'',reason:''};})};
      var c=a.costCoverage,serial=0;
      var categories={travel_labor:'Driver work',fuel_energy:'Fuel or energy',vehicle:'Vehicle cost',fees:'Fees',parking:'Parking',tolls:'Tolls',maintenance:'Maintenance',consumables:'Supplies',mobilization:'Mobilization',access:'Site access',other:'Other cost'};
      function field(host,label,row,key,items,redraw){var wrap=node('div',null,host),l=node('label',label,wrap),e=node(items?'select':'input',null,wrap);e.id='cdPreparedCoverage-'+serial++;l.htmlFor=e.id;if(items){[['','Choose a cost']].concat(items).forEach(function(x){var o=node('option',x[1],e);o.value=x[0];});}else{e.type='text';e.maxLength=key==='reason'?500:20;if(key==='amount')e.inputMode='decimal';}e.value=row[key];e.addEventListener(items?'change':'input',function(){row[key]=e.value;if(redraw)redraw();changed();if(redraw){var id=e.id;coverageFields(info);document.getElementById(id)?.focus();}});return e;}
      function pairs(lines){return(lines||[]).map(function(l){return[l.id,l.label];});}
      function category(host,row,lineId,key){var line=info.travelLines.find(function(l){return l.id===lineId;});field(host,'Travel cost type',row,key,(line?.categories||[]).map(function(v){return[v.category,categories[v.category]||v.category.replaceAll('_',' ')];}));}
      c.overlaps.forEach(function(r,i){var group=node('fieldset',null,coverageBody);node('legend','Travel Cost Already Included',group);field(group,'Travel item',r,'travelLineId',pairs(info.travelLines),function(){r.category='';});category(group,r,r.travelLineId,'category');field(group,'Included in',r,'component',[['labor','Work costs'],['equipment','Equipment costs']],function(){r.sourceLineId='';});field(group,'Covering cost',r,'sourceLineId',pairs(info.costLines[r.component]));field(group,'Amount already included',r,'amount');field(group,'Where this amount is included',r,'reason');button('Remove Allocation','cdPreparedCoverageRemove-'+i,function(){c.overlaps.splice(i,1);coverageFields(info);changed();coverageBox.querySelector('summary').focus();},group);});
      var add=button('Add Included Travel Cost','cdPreparedCoverageAdd',function(){c.overlaps.push({travelLineId:'',category:'',component:'labor',sourceLineId:'',amount:'',reason:''});coverageFields(info);changed();coverageBody.querySelector('fieldset:last-of-type select')?.focus();},coverageBody);add.disabled=c.overlaps.length>=24||!info.travelLines.length;
      c.equipmentOutside.forEach(function(r){var fact=info.equipmentOutside.find(function(x){return x.equipmentLineId===r.equipmentLineId&&x.kind===r.kind;}),group=node('fieldset',null,coverageBody);node('legend',(fact?.label||'Equipment')+' - '+(r.kind==='operator'?'Operator work':'Travel'),group);node('p',options.money(r.amount,review.currency)+' is excluded from equipment costs. Identify where it is covered.',group);field(group,'Covering cost',r,'targetLineId',pairs(r.component==='labor'?info.costLines.labor:info.travelLines),function(){if(r.component==='travel')r.targetCategory='';});if(r.component==='travel')category(group,r,r.targetLineId,'targetCategory');field(group,'Where this share is included',r,'reason');});
      c.travelOutside.forEach(function(r){var fact=info.travelOutside.find(function(x){return x.travelLineId===r.travelLineId;}),group=node('fieldset',null,coverageBody);node('legend',(fact?.label||'Travel')+' - other vehicle costs',group);field(group,'Covering equipment cost',r,'sourceLineId',pairs(info.costLines.equipment));field(group,'Amount already included',r,'amount');field(group,'Which vehicle costs this covers',r,'reason');});
      if(c.equipmentOutside.length||c.travelOutside.length)coverageBox.open=true;
    }
    function allocationFields(items){var old={};allocations.querySelectorAll('input').forEach(function(e){old[e.dataset.reference]=e.value;});allocations.replaceChildren();if(overhead.value!=='allocated')return;(items||[]).forEach(function(item,i){var label=node('label',item.label+' — included overhead',allocations);label.htmlFor='cdPreparedIncluded-'+i;var input=node('input',null,allocations);input.id=label.htmlFor;input.type='text';input.inputMode='decimal';input.maxLength=20;input.dataset.reference=item.referenceId;input.value=old[item.referenceId]||'';input.oninput=changed;});if(!items?.length)node('p','Calculate once to load the reviewed work costs, then enter the included amounts.',allocations,'prepared-muted');}
    if(review.simulated){overhead.value='disjoint';costCheck.checked=true;costLabel.hidden=true;}
    coverageFields(null);
    overhead.onchange=function(){allocationFields(a.overheadOptions);changed();};costCheck.onchange=changed;policyCheck.onchange=changed;consent.onchange=update;
    function draftBody(){if(!eligible()||!costCheck.checked)throw{status:400,message:'Confirm that the costs are counted once before reviewing the total.'};var selection={};groups.forEach(function(g){selection[g[0]]=choices[g[0]].value;});var included=[];allocations.querySelectorAll('input').forEach(function(e){if(e.value.trim())included.push({referenceId:e.dataset.reference,amount:e.value.trim()});});
      var coverageStatus=overhead.value==='allocated'&&!included.length?'unknown':overhead.value;
      var savedPolicy=review.pricingPolicies?.current;
      var auditReason='Owner reviewed the prepared estimate, plan selections, cost coverage, and proposed price.';
      return{version:'estimate-proposal-adoption-v1',draft:copy(state.currentDraft),selection:selection,previousReceipt:a.historyCurrent?{id:a.historyCurrent.id,revision:a.historyCurrent.revision,digest:a.historyCurrent.digest}:null,coverage:{costs:Object.assign({overlaps:[],equipmentOutside:[],travelOutside:[]},copy(a.costCoverage||{}),{confirmed:true,reason:auditReason}),overhead:{status:coverageStatus,explanation:coverageStatus==='unknown'?'':auditReason,included:included}},pricingPolicy:policyCheck.checked?{inputs:copy(savedPolicy.inputs),sourceDigest:review.pricingPolicies.sources.digest,currentPin:{id:savedPolicy.id,revision:savedPolicy.revision,digest:savedPolicy.digest}}:null,expectedReviewDigest:null,confirmed:false,reason:auditReason};
    }
    function fetchData(suffix,method,body,key,demoRevision){var headers={'Content-Type':'application/json'};if(key)headers['Idempotency-Key']=key;if(Number.isSafeInteger(demoRevision))headers['X-NorthStar-Demo-Revision']=String(demoRevision);return window.NorthStarAccountSession.fetch('/api/v1/canonical/estimates/'+encodeURIComponent(review.pins.estimateId)+'/'+suffix,{method:method,headers:headers,body:body?JSON.stringify(body):undefined}).then(function(r){return r.json().catch(function(){return{};}).then(function(b){if(!r.ok||!b.success||!b.data)throw{status:r.ok?503:r.status,message:b.error?.message,code:String(b.error?.code||b.error?.category||'').toUpperCase()};return b.data;});});}
    function error(e,saving){if([401,403,404,409,410].indexOf(e.status)>=0)state.stale=true;if([409,410].indexOf(e.status)>=0){message.textContent='Updating this estimate with the latest job and business information…';setTimeout(function(){if(current())options.refresh();},0);return;}message.textContent=e.status===401?'Sign in again to review this estimate.':e.status===403?'Your current account cannot save this estimate.':e.status===404?'This estimate is unavailable. Refresh and choose an available estimate.':e.status===413?'Shorten the notes before reviewing again.':e.status===429?(['PROPOSAL_ADOPTION_HISTORY_LIMIT','PROPOSAL_ADOPTION_STORAGE_LIMIT','DEMO_SESSION_LIMIT','DEMO_WORK_CAPACITY'].indexOf(e.code)>=0?(review.simulated?'This demo has reached its saved-work limit. History remains available. Resetting the demo clears its practice work.':'Saved estimate history cannot accept another entry. Existing history remains available.'):'Saving is limited. Review the current sources and saved history before trying again.'):[400,422].indexOf(e.status)>=0?(e.message||'Review the costs, sources and confirmation before calculating again.'):e.code==='PROPOSAL_ADOPTION_PAUSED'?'New prepared estimates are paused. Refresh to check saved history.':saving?'The save result is uncertain. Retry the same save or refresh to check history.':'The review is unavailable. Try calculating again.';}
    function preview(){var body;try{body=draftBody();}catch(e){error(e,false);message.focus();return;}var sequence=++a.sequence,trigger=document.activeElement;a.busy=true;a.value=null;consent.checked=false;update();message.textContent='Reviewing the combined estimate…';fetchData('proposal-adoption-preview','POST',body).then(function(data){if(!current()||sequence!==a.sequence)return;if(data.version!=='estimate-proposal-adoption-v1'||Date.parse(data.expiresAt)<=Date.now())throw{status:410};a.body=body;a.value=data;introduction.hidden=data.state==='ready';a.coverageOptions=data.coverageOptions||null;coverageFields(a.coverageOptions);a.overheadOptions=data.overheadOptions||[];allocationFields(a.overheadOptions);result.replaceChildren();node('h5','Your Prepared Estimate',result);if(data.price){var hero=node('div',null,result,'prepared-total'),profit=data.price.proposedBeforeTax!==null&&data.price.costWithOverhead!==null?(Number(data.price.proposedBeforeTax)-Number(data.price.costWithOverhead)).toFixed(2):null;row('Proposed price before tax',data.price.proposedBeforeTax,hero);row('Operating cost',data.price.costWithOverhead,hero);row('Estimated profit',profit,hero);}else row('Total direct cost',data.completeCost);node('p','Original estimate kept · Price approval still required',result,'prepared-muted');var breakdown=node('details',null,result);node('summary','Cost Breakdown',breakdown);row('Current recorded cost',review.financialCosts?.knownDirectCosts??review.pricingPlans?.sources?.basis?.directCosts??null,breakdown);node('h6','Prepared direct costs',breakdown);if(data.financialCosts){row('Materials',data.financialCosts.knownDirectMaterialCost,breakdown);row('Work and crew',data.financialCosts.knownInternalLaborCost,breakdown);row('Equipment',data.financialCosts.knownEquipmentCost,breakdown);row('Travel and logistics',data.financialCosts.knownTravelInternalCost,breakdown);}row('Direct-cost subtotal',data.completeCost,breakdown).classList.add('prepared-subtotal');row('Additional overhead',data.price?.overhead.incremental??null,breakdown);row('Operating cost',data.price?.costWithOverhead??null,breakdown).classList.add('prepared-grand-total');if(data.price){var detailProfit=data.price.proposedBeforeTax!==null&&data.price.costWithOverhead!==null?(Number(data.price.proposedBeforeTax)-Number(data.price.costWithOverhead)).toFixed(2):null;row('Estimated profit',detailProfit,breakdown).classList.add('prepared-grand-total');}
      var includedTravel=data.financialCosts?.travelAlreadyIncluded,includedOverhead=data.price?.overhead.alreadyIncluded,outside=data.coverageOptions?.equipmentOutside||[],vehicleOutside=data.coverageOptions?.travelOutside||[];
      if(Number(includedTravel)>0||Number(includedOverhead)>0||outside.length||vehicleOutside.length||(data.price&&data.price.overhead.gross===null)){var mechanics=node('details',null,breakdown,'prepared-calculation-details');node('summary','How this total was calculated',mechanics);if(Number(includedTravel)>0){row('Travel before included costs',data.financialCosts.travelGrossCost,mechanics);row('Travel already included elsewhere',includedTravel,mechanics);}if(Number(includedOverhead)>0||data.price?.overhead.gross===null){row('Gross overhead',data.price?.overhead.gross??null,mechanics);row('Overhead already included',includedOverhead,mechanics);}outside.forEach(function(x){row(x.label+' - '+(x.kind==='operator'?'operator work':'travel')+' covered elsewhere',x.amount,mechanics);});if(vehicleOutside.length)node('p','Other vehicle costs use the explicit equipment allocations reviewed under Plan Choices And Cost Review.',mechanics,'prepared-muted');}
      if(data.policy)row('Pricing-policy minimum',data.policy.threshold,breakdown);var ownsFocus=document.activeElement===trigger;settings.open=data.state!=='ready';if(ownsFocus)message.focus();message.textContent=data.state==='ready'?'Ready for your review.':data.incompleteReason==='coverage'?'Complete the included-cost allocations, then review totals again.':'Enter the included overhead amounts, then review totals again.';}).catch(function(e){if(current()&&sequence===a.sequence){a.value=null;a.body=null;result.replaceChildren();error(e,false);}}).finally(function(){if(current()&&sequence===a.sequence){a.busy=false;update();if(document.activeElement===trigger)message.focus();}});}
    function send(attempt){if(a.busy||!attempt)return;var sequence=++a.sequence;a.busy=true;update();message.textContent='Saving the prepared estimate…';fetchData('proposal-adoptions','POST',attempt.body,attempt.key,attempt.demoRevision).then(function(data){if(!current()||sequence!==a.sequence)return;a.pending=null;persist();a.value=null;a.body=null;consent.checked=false;message.textContent='Prepared estimate saved. Review and approve its price separately.';options.refresh();}).catch(function(e){if(!current()||sequence!==a.sequence)return;if([400,401,403,404,409,410,413,422,429].indexOf(e.status)>=0||(e.status===503&&e.code==='PROPOSAL_ADOPTION_PAUSED')){a.pending=null;persist();a.value=null;a.body=null;consent.checked=false;}error(e,true);}).finally(function(){if(current()&&sequence===a.sequence){a.busy=false;update();message.focus();}});}
    fetchData('proposal-adoptions','GET').then(function(data){if(!current())return;a.historyCurrent=data.current;a.historyLoaded=true;update();historyBody.replaceChildren();if(!data.history.length)node('p','No prepared estimates have been saved.',historyBody,'prepared-muted');data.history.forEach(function(r){node('p','Saved '+new Date(r.createdAt).toLocaleString()+' by '+r.actorName+'. Price approval is separate.',historyBody);});}).catch(function(){if(current()){a.historyCurrent=undefined;message.textContent='Saved history could not be loaded. Refresh before combining plans.';calculate.disabled=true;}});
    if(a.pending)message.textContent='A prior save has an uncertain result. Retry that same save or refresh to check history.';
    box.addEventListener('toggle',function(){var overview=document.getElementById('cdPreparedOverview');if(overview)overview.hidden=!!a.value&&box.open;});
    update();return{update:update,invalidate:changed};
  }
  function mount(review, host, previous, options) {
    dispose(previous);
    var state = previous && previous.estimateId === review.pins.estimateId ? previous : { estimateId: review.pins.estimateId, entries: {}, sequence: 0 };
    state.candidateIds=state.candidateIds||[];
    state.data = null; state.busy = false; state.acknowledged = false; state.stale = true;
    var root = node('section', null, host, 'prepared-estimate'); root.id = 'cdPreparedEstimate'; root.setAttribute('aria-labelledby', 'cdPreparedTitle');
    var header = node('div', null, root, 'prepared-heading'); node('h4', 'Polaris Estimate Builder', header).id = 'cdPreparedTitle';
    node('p', 'Prefilled from the recorded call, job scope, business profile, crew, vehicles, equipment, tools and cost policies.', root, 'prepared-muted');
    var status = node('p', 'Preparing the estimate…', root, 'prepared-status'); status.id = 'cdPreparedStatus'; status.setAttribute('role', 'status'); status.tabIndex = -1;
    var actions = node('div', null, root, 'prepared-actions');
    function button(label, id, fn, parent) { var b = node('button', label, parent || actions, 'btn btn-secondary'); b.type = 'button'; b.id = id; b.addEventListener('click', fn); return b; }
    var view = button('View Draft Estimate', 'cdPreparedViewEstimate', function () { var target=document.getElementById('cdBtnEstimate');if(target)target.click(); }); view.className='btn btn-primary';
    var refresh = button('Refresh Calculation', 'cdPreparedRefresh', function () { options.refresh(); });
    var questions = node('div', null, root); questions.id = 'cdPreparedQuestions';
    var edit = node('details', null, root, 'prepared-inputs'); var editSummary = node('summary', 'Adjust Draft Details', edit); editSummary.id = 'cdPreparedAdjust';
    node('p', 'Add technical details when you know them. These assumptions apply only to this draft.', edit, 'prepared-muted');
    var form = node('form', null, edit); form.addEventListener('submit', function (e) { e.preventDefault(); });
    var fields = node('div', null, form, 'prepared-fields'); var formActions = node('div', null, form, 'prepared-actions');
    var calculate = button('Recalculate', 'cdPreparedCalculate', function () { request(false); }, formActions);
    button('Clear Changes', 'cdPreparedClear', function () { state.entries = {};state.candidateIds=[]; invalidate('Changes cleared. ' + (state.stale ? 'Refresh before calculating again.' : 'Recalculate to use the recorded details.')); renderFields(); if(edit.hidden)status.focus();else editSummary.focus(); }, formActions);
    button('Cancel Changes', 'cdPreparedCancel', function () { state.entries = copy(state.applied || {});state.candidateIds=(state.appliedCandidates||[]).slice(); invalidate(state.stale ? 'Refresh before calculating again.' : 'Draft edits canceled. Recalculate when ready.'); renderFields(); edit.open = false; editSummary.focus(); }, formActions);
    var planDetails=node('details',null,root,'prepared-plan-details');node('summary','Plans And Sources',planDetails);
    var results = node('div', null, planDetails, 'prepared-groups'); results.id = 'cdPreparedGroups';
    var groupHosts = {};
    groups.forEach(function (g) {
      var section = node('section', null, results, 'prepared-group'); node('h5', g[1], section); groupHosts[g[0]] = node('div', null, section);
      button('Edit ' + g[1], 'cdPreparedEdit-' + g[0], function () {
        var target = document.getElementById(g[2]); if (!target) return;
        for (var ancestor = target; ancestor; ancestor = ancestor.parentElement) if (ancestor.tagName === 'DETAILS') ancestor.open = true;
        var control = target.querySelector('summary,button,input,select'); if (control) { control.focus(); control.scrollIntoView({ block: 'nearest' }); }
      }, section);
    });
    var acknowledgment = node('label', null, planDetails, 'prepared-ack'); var check = node('input', null, acknowledgment); check.type = 'checkbox'; check.id = 'cdPreparedAcknowledge';
    node('span', 'I have reviewed these draft assumptions. This does not approve a price or save plans.', acknowledgment);
    check.addEventListener('change', function () { state.acknowledged = check.checked; });
    var overview=node('div',null,root,'prepared-overview');overview.id='cdPreparedOverview';root.insertBefore(overview,questions);
    function current() { return options.isCurrent() && root.isConnected; }
    var adoption=adoptionPanel(review,root,state,options,current);root.insertBefore(document.getElementById('cdPreparedAdoption'),questions);
    function controls() { calculate.disabled = state.busy || state.stale || state.unmatched || !state.fields || !state.fields.length; refresh.disabled = state.busy; check.disabled = !state.data || state.stale || state.dirty || state.data.readiness === 'blocked'; check.checked = !!state.acknowledged && !check.disabled; if(adoption)adoption.update(); }
    function invalidate(message) { state.sequence++; state.busy = false; state.data = null; state.dirty = true; state.acknowledged = false; clearTimeout(state.timer);var expiry=state.expiresAt;if(expiry&&Date.parse(expiry)<=Date.now())state.stale=true;else if(expiry)state.timer=setTimeout(function(){if(current()&&state.expiresAt===expiry)options.refresh();},Math.max(0,Date.parse(expiry)-Date.now()));status.textContent = message; renderGroups(); controls(); }
    function amount(value) { return options.money(value, review.currency); }
    function row(parent, label, value) { var r = node('div', null, parent, 'prepared-row'); node('span', label, r); node('strong', value == null ? 'Not recorded' : String(value), r); }
    function renderGroups() {
      if(overview){
        overview.replaceChildren();
        var draftPrice=state.data&&state.data.components.find(function(c){return c.kind==='pricing';}),priceValue=draftPrice&&draftPrice.result&&(draftPrice.result.result||draftPrice.result),originalRow=(review.rows||[])[0],originalPrice=originalRow&&originalRow.amount,preparedPrice=priceValue&&priceValue.proposedBeforeTax;
        row(overview,'Original Polaris estimate',originalPrice==null?'Needs review':amount(typeof originalPrice==='number'?originalPrice.toFixed(2):originalPrice));
        row(overview,'Prepared draft before tax',preparedPrice==null?'Needs review':amount(preparedPrice));
        if(originalPrice!=null&&preparedPrice!=null&&Number.isFinite(Number(originalPrice))&&Number.isFinite(Number(preparedPrice))){
          var delta=Math.round((Number(preparedPrice)-Number(originalPrice))*100)/100;
          node('p',Math.abs(delta)<.005?'No price change. The prepared draft currently matches the original Polaris estimate.':'Prepared draft change: '+(delta>0?'+':'-')+amount(Math.abs(delta).toFixed(2))+'. Review the adjusted details and plans below.',overview,'prepared-muted');
        }else node('p','Review the costs and remaining questions before saving.',overview,'prepared-muted');
      }
      groups.forEach(function (g) {
        var target = groupHosts[g[0]]; target.replaceChildren(); var c = state.data && state.data.components.find(function (c) { return c.kind === g[0]; });
        if(state.denied){node('p','Current information is unavailable.',target,'prepared-muted');return;}
        if(g[0]==='pricing'){var decision=review.decisions&&review.decisions.current,approved=decision&&decision.action==='approve'?decision:(review.decisions&&review.decisions.history||[]).find(function(d){return d.action==='approve';});row(target,'Last approved price before tax',approved?amount(approved.priceBeforeTax):'No approved price recorded');if(approved&&approved!==decision)node('p','This approval belongs to an earlier saved review.',target,'prepared-muted');}
        if (!c) { node('p', state.stale ? 'Refresh to check current information.' : state.dirty ? 'Recalculate to update this group.' : 'More job or company information is needed.', target, 'prepared-muted'); return; }
        if (c.origin === 'current_saved_plan') node('p', 'Saved plan retained. Use Edit to review it.', target, 'prepared-muted');
        if (!c.inputs || !c.result) { node('p', 'Current information is unavailable. Review the saved plan under Edit.', target); return; }
        if (g[0] === 'materials') {
          (c.inputs.lines || []).forEach(function (l) { row(target, l.material, l.quantity + ' ' + l.unit); row(target, 'Waste allowance', l.wastePercent + '%'); }); row(target, 'Material cost', amount(c.result.total));
        } else if (g[0] === 'labor') {
          (c.result.lines || []).forEach(function (l, i) { var input = c.inputs.lines[i]; row(target, input.task, l.workerHours == null ? 'Work time needs review' : l.workerHours + ' worker-hours'); }); row(target, 'Labor cost', amount(c.result.total));
          node('p', 'Work time is not elapsed job time or a crew assignment.', target, 'prepared-muted');
          var candidates = state.data.candidates && state.data.candidates.workers || []; var details = node('details', null, target); node('summary', 'Crew Review', details);
          if (!candidates.length) node('p', 'No current crew information is available.', details);
          candidates.forEach(function (v) { node('p', v.label + ' — ' + (v.status === 'blocked' ? 'Recorded requirements or schedule conflict' : v.status === 'eligible_for_review' ? 'Review for the recorded job time; not assigned' : 'Requirements or availability need review'), details); });
        } else if (g[0] === 'equipment') {
          (c.inputs.lines || []).forEach(function (l, i) { row(target, l.task, l.identity && (l.identity.displayName || l.identity.model || l.identity.equipmentType) || 'Equipment details need review'); var result = (c.result.lines || [])[i]; node('p', result && result.status === 'matches_reviewed_requirements' ? (review.simulated?'Prefilled from this simulated business inventory and matched to the recorded job requirements.':'Recorded specifications match the reviewed requirements. Check current condition and availability before dispatch.') : 'Confirm requirements and current equipment information.', target, 'prepared-muted'); var cost = (c.costs || []).find(function (v) { return v.lineId === l.lineId; }); row(target, 'Equipment cost', cost ? amount(cost.total) : 'Unavailable'); });
        } else if (g[0] === 'travel') {
          (c.inputs.logistics||[]).filter(function(item){return /^Vehicle plan/.test(item.label);}).forEach(function(item){row(target,item.label.replace(/^Vehicle plan · /,''),'Selected from Business Profile fleet');});
          row(target, 'Travel and logistics cost', amount(c.result.total)); node('p', 'Vehicle cost is counted here and is not charged again in equipment.', target, 'prepared-muted');
        } else {
          var price = c.result.result || c.result; row(target, 'Proposed price before tax', amount(price.proposedBeforeTax));
          if (price.overhead) { row(target, 'Gross overhead', amount(price.overhead.gross)); row(target, 'Already included', amount(price.overhead.alreadyIncluded)); row(target, 'Additional overhead', amount(price.overhead.incremental)); }
          node('p', 'Complete cost and margin remain unavailable until included costs are reconciled.', target, 'prepared-muted');
        }
        if (c.origin === 'current_saved_plan') node('p', "Review this saved plan's sources under Edit.", target, 'prepared-muted');
        else if (state.data.recipe) { var source = node('details', null, target); node('summary', 'Planning Source', source); node('p', state.data.recipe.label || 'Company planning information', source); node('p', 'Effective ' + state.data.recipe.effectiveOn + ' · Review by ' + state.data.recipe.reviewBy, source); }
      });
    }
    function renderQuestions() {
      questions.replaceChildren(); if (!state.data) return;
      var conflicts = state.data.conflicts || []; conflicts.forEach(function (text) { node('p', text, questions, 'prepared-status'); });
      var items = (state.data.questions || []).filter(function(q){return ['resource_readiness','cost_coverage'].indexOf(q.id)<0;}); if (!items.length) return; node('h5', 'What Needs Your Input', questions);
      function add(q, parent) { var item = node('div', null, parent, 'prepared-question'); node('strong', q.question, item); node('p', q.id==='cost_coverage'?'These draft costs remain separate. Review any costs already included before changing the price.':q.why, item, 'prepared-muted'); }
      items.slice(0, 3).forEach(function (q) { add(q, questions); }); if (items.length > 3) { var rest = node('details', null, questions); node('summary', 'More Details To Review', rest); items.slice(3).forEach(function (q) { add(q, rest); }); }
    }
    function renderFields() {
      if(state.denied){fields.replaceChildren();edit.hidden=true;return;}
      fields.replaceChildren(); var all = state.fields || [];state.unmatched=Object.keys(state.entries).some(function(id){return !all.some(function(f){return f.id===id;});})||state.candidateIds.some(function(id){return !(state.workers||[]).some(function(w){return w.id===id;});}); edit.hidden = !all.length&&!Object.keys(state.entries).length&&!state.candidateIds.length;
      if(state.unmatched)status.textContent='Some earlier edits no longer match the current planning information. Open Adjust Draft Details and use Clear Changes before recalculating.';
      all.forEach(function (f, i) {
        var wrapper = node('div', null, fields); var label = node('label', f.label + (f.unit === 'category' ? '' : ' (' + f.unit + ')'), wrapper); label.htmlFor = 'cdPreparedField-' + i;
        var input = node(f.type === 'category' ? 'select' : 'input', null, wrapper); input.id = label.htmlFor;
        if (f.type === 'category') { var blank = node('option', 'Use recorded detail', input); blank.value = ''; f.allowedValues.forEach(function (v) { var option = node('option', v, input); option.value = v; }); }
        else { input.type = 'text'; input.inputMode = 'decimal'; input.maxLength = 40; input.placeholder = 'Use recorded detail'; }
        input.value = state.entries[f.id] ? state.entries[f.id].value : '';
        var reasonLabel = node('label', 'Reason For This Assumption', wrapper); reasonLabel.htmlFor = input.id + '-reason'; var reason = node('input', null, wrapper); reason.id = reasonLabel.htmlFor; reason.type = 'text'; reason.maxLength = 500; reason.value = state.entries[f.id] ? state.entries[f.id].reason : '';
        function change() { if (input.value === '') delete state.entries[f.id]; else state.entries[f.id] = { fieldId: f.id, value: input.value, unit: f.unit, sourceKind: 'owner_assumption', reason: reason.value }; invalidate(state.stale ? 'Information changed. Refresh before calculating again.' : 'Unsaved changes. Recalculate to update the draft.'); questions.replaceChildren(); }
        input.addEventListener('input', change); input.addEventListener('change', change); reason.addEventListener('input', change);
      });
      if(all.length&&(state.workers||[]).length){var label=node('label','Crew To Review (Not Assigned)',fields);label.htmlFor='cdPreparedCrew';var select=node('select',null,label);select.id='cdPreparedCrew';var none=node('option','No crew selected',select);none.value='';state.workers.forEach(function(w){var option=node('option',w.label+(w.status==='blocked'?' — Conflict':' — Review needed'),select);option.value=w.id;});select.value=state.candidateIds[0]||'';select.addEventListener('change',function(){state.candidateIds=select.value?[select.value]:[];invalidate(state.stale?'Refresh before reviewing this crew.':'Crew choice changed. Recalculate to check the current requirements.');});}
      controls();
    }
    function request(initial) {
      if (!current()) return;if(!initial&&state.unmatched)return; if (!initial && (state.stale || Date.parse(state.expiresAt) <= Date.now())) { state.stale=true;status.textContent='Updating this estimate with the latest job and business information…';options.refresh();return; }
      var overrides = initial ? [] : Object.keys(state.entries).map(function (key) { return state.entries[key]; });
      if (overrides.some(function (v) { return !v.reason.trim(); })) { status.textContent = 'Add a reason for each changed detail.'; status.focus(); return; }
      var sequence = ++state.sequence, trigger = document.activeElement;
      state.busy = true; state.data = null; state.acknowledged = false; clearTimeout(state.timer); renderGroups(); controls(); status.textContent = initial ? 'Preparing the current estimate…' : 'Recalculating your draft…';
      var body = { version: 'estimate-proposal-preview-v1', selectedRevision: review.selectedRevision || null, expectedBasisDigest: initial ? null : state.basis, overrides: overrides, candidateIds: initial?[]:state.candidateIds.slice() };
      window.NorthStarAccountSession.fetch('/api/v1/canonical/estimates/' + encodeURIComponent(review.pins.estimateId) + '/proposal-preview', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })
        .then(function (r) { return r.json().catch(function () { return {}; }).then(function (b) { if (!r.ok) throw { status: r.status }; if (!b.success || !b.data || b.data.version !== 'estimate-proposal-preview-v1') throw { status: 503 }; return b.data; }); })
        .then(function (data) {
          if (!current() || state.sequence !== sequence) return;
          if (data.sourcePins.estimateId !== review.pins.estimateId || JSON.stringify(data.sourcePins) !== JSON.stringify(review.pins)) throw { status: 409 };
          if (Date.parse(data.expiresAt) <= Date.now()) throw { status: 410 };
          state.currentDraft=Object.assign({},body,{expectedBasisDigest:data.basisDigest});state.denied=false;state.data = data; state.basis = data.basisDigest; state.expiresAt=data.expiresAt; state.stale = false; state.fields = data.editableFields || (initial?[]:state.fields);state.workers=data.candidates&&data.candidates.workers||[]; state.dirty = initial && (Object.keys(state.entries).length > 0||state.candidateIds.length>0);
          if (!initial) {state.applied = copy(state.entries);state.appliedCandidates=state.candidateIds.slice();}
          status.textContent = state.dirty ? 'Current information refreshed. Your edits are retained; recalculate to apply them.' : data.readiness === 'blocked' ? 'Resolve the conflicting job details before using this draft.' : data.components.length ? 'Draft prepared. Review the assumptions and remaining questions.' : 'More information is needed. Start with the questions below.';
          renderGroups(); renderQuestions(); if (initial) renderFields();
          state.timer = setTimeout(function () { if (current() && state.expiresAt === data.expiresAt) options.refresh(); }, Math.max(0, Date.parse(data.expiresAt) - Date.now()));
        }).catch(function (error) {
          if (!current() || state.sequence !== sequence) return;
          state.data = null; state.acknowledged = false;state.denied=[401,403,404].indexOf(error.status)>=0; if ([401, 403, 404, 409, 410].indexOf(error.status) >= 0) state.stale = true;
          if([409,410].indexOf(error.status)>=0){status.textContent='Updating this estimate with the latest job and business information…';setTimeout(function(){if(current())options.refresh();},0);}else status.textContent = error.status === 401 ? 'Sign in again to prepare this estimate.' : error.status === 403 ? 'Your current account cannot prepare this estimate.' : error.status === 404 ? 'This estimate is unavailable. Choose a current estimate.' : error.status === 400 ? 'Check the details, matching units and reasons, then recalculate.' : error.status === 413 ? 'Shorten the assumption notes and try again.' : error.status === 429 ? 'Preparation is temporarily limited. Wait and try again.' : 'Preparation is unavailable. Try again; no estimate changes were saved.';
          renderGroups();if(state.denied)renderFields(); questions.replaceChildren();
        }).finally(function () { if (!current() || state.sequence !== sequence) return; state.busy = false; controls(); if (document.activeElement === trigger && (trigger === refresh || trigger === calculate)) status.focus(); });
    }
    renderGroups(); renderFields(); controls();
    if (review.isCurrent === false) status.textContent = 'Choose the current estimate to prepare a new draft.'; else request(true);
    return state;
  }
  window.NorthStarPreparedEstimate = { mount: mount, dispose: dispose };
}());
