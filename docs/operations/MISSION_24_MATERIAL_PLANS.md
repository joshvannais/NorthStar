# Mission24 Part2 Slice2 — single-material plans

This bounded slice adds actual internal material quantity/waste/unit-cost planning to the existing private estimate drawer. A current owner/admin can calculate on the server, save, revise, withdraw and reload an attributed single-material plan with immutable history. It is not an approved customer quote, inventory/reservation or automatic adoption into the recorded estimate. Existing057 remains the human scope/price decision; no approval is transferred to material inputs. Existing canonical costs, snapshots, price and Capella comparison remain unchanged.

## Calculation and source contract

src/estimating/materialPlanContract.js owns estimate-material-plan-v1 validation and exact scaled-integer math, shared by paid/demo preview and saved projection. Required quantity is positive, at most9whole/6decimal digits; waste0–100% at most2decimals; unit price is nonnegative exact2decimal currency text, at most12whole digits. Supported currencies USD/CAD/EUR must match the selected estimate. Missing values are not zero. Result above999999999999.99 is rejected. No binary floating-point multiplication supplies money authority.

Unit vocabulary: ea Items; m Metres; m2 Square metres; m3 Cubic metres; ft Feet; ft2 Square feet; ft3 Cubic feet; yd3 Cubic yards; kg Kilograms; lb Pounds; l Litres; gal US liquid gallons (US liquid gallon,231cubic inches). No conversions, supplier package rounding or inferred units. Changing unit clears quantity and unit price, requiring explicit new entries and calculation. Count quantity must be a whole number; waste-inclusive count rounds up to whole items. Other waste-inclusive quantities round upward to6decimal places. Multiply that planned quantity by unit-price cents and round half-up to cents once. Preserve required/additional/planned quantity, displayed rate and rounding explanation.

Examples:100ft with10% waste at3.25/ft gives110ft and357.50;3items with10% waste at2.50 gives4items and10.00; explicit zero price gives0.00;0.333333m at0.01 and0waste rounds cost to0.00;0.000001m plus0.01% waste rounds planned quantity upward to0.000002m. The rule is disclosed, not hidden. This is one-material planning, not full BOM, market accuracy, tax, markup or procurement.

Material description is1–160Unicode code points; source note1–1000; controls are rejected. User selects My cost estimate or Price information I entered. Optional valid price date remains a human assertion; blank stays unknown. No supplier proof, live data, availability or freshness guarantee is inferred. Generic scope measurements, the prior flat profile cost map and Mission23 movements are not consumed as unit prices or stock.

## Durable authority and concurrency

Additive058 creates canonical_material_plans, immutable save/withdraw history under tenant/estimate foreign keys. Each event retains exact source pins, expected human decision revision/digest (or explicit absence), actor/membership/session/time, input JSON, calculation version, confirmation version, reason, previous event/revision/digest and actor-scoped idempotency hashes. Current read history is capped20 with retained total/truncation; revisions cap10000. No existing migration is edited. Runtime direct ledger/helper privileges are withheld; only bounded read/mutate entry points are granted and verified. SQL independently validates input shapes/ranges/date/arithmetic overflow; the browser cannot submit an authoritative total.

Current owner/admin/session/account/subscription/CSRF authority is checked before a paid replay and again after waits. Mutation is SERIALIZABLE, uses the selected estimate lock also used by human decisions, and2s lock/10s statement limits. Expected plan/source/human decision bases must match for new events; same actor/key/body replays the original receipt without reactivating superseded state, changed body conflicts. Withdrawal appends history. No quote, price or canonical calculation write is performed.

Preview at POST /api/v1/canonical/estimates/:estimateId/material-plan-preview validates current private read authority and source/decision/plan basis without persistence. POST /material-plans adds explicit confirmed save/withdraw. Both use the existing bounded unambiguous JSON body boundary. Existing GET /review includes current plans/history and whether its human decision basis is current. Missing human decision is stated explicitly, not called approval. A changed human decision does not approve the plan or silently rewrite old source; the UI preserves useful drafts but clears calculation/confirmation and requires renewed calculation/review.

Server preview is authoritative; no independent browser calculator. Every relevant field edit clears the prior preview/confirmation. Requests retain exact attempted bodies for retries. Refresh/customer switching discards outdated responses; cancel and save return useful focus. Known401/403/409/429/503 failures receive safe action guidance; unknown outcomes retain same-attempt retry guidance. Raw backend messages/IDs/versions are not rendered.

Demo uses the same calculation/validation/projector/form with separate token-owned materialPlans history in existing workspace state and distinct material_plan mutation receipts.058 adds only that operation to the existing demo check. Session revision/expiry/reset and24action cap remain; at most20plan events per estimate within that cap. No private endpoint call or paid authority is conferred. Ordinary calculate/save/reload/revise/withdraw/reset must work, preserving older graphs and human decisions unless explicitly reset.

## Startup and recovery gates

058 is a NEW schema risk. Its DDL includes the new ledger/functions and replacement/validation of the existing demo operation check, which needs an ACCESS EXCLUSIVE table lock. Startup explicitly extends the existing tighter-of-inherited5s lock/20s perstatement transaction-local guard to058 before advisory acquisition through commit/rollback. This is not a total deployment deadline or a guarantee of zero downtime. Generic runtime DML verification excludes only the new protected ledger; dedicated verification checks its withheld privileges and narrow entry points.

Source-controlled recovery/material-plans-read-only.patch disables new paid/demo material mutations including replay, preserving saved plan/source/history and human decision reads. recovery/estimate-material-combined-read-only.patch also disables existing human decision mutations. Material-only pause leaves existing human decision authority intact; combined pause truthfully identifies both. Preview is nonpersistent and may remain available to authorized API readers, while UI is read-only. Quiesce all normal writers before operational routing to either recovery build; old pre058 code is not an acceptable truthful plan-history recovery. Never drop history or edit applied058. Normal forward resume appends the next revision.

tests/api/m24-material-plan-upgrade-recovery.js populates exact released7072643 with estimates and paid/demo human decisions, upgrades actual startup through058, saves paid/demo plans, mounts both actual source-derived pauses with Chrome/WebKit views, checks immutable history, and resumes normally. tests/api/m24-material-plan-startup-timeouts.js exercises benign ordinary table/advisory contention, statement delay, rollback, tighter settings restoration, apply-once and zero-op. These are disposable application/schema rehearsals, not physical backup restoration, production topology/draining, RPO/RTO or provider evidence. A new concrete production readiness disposition belongs to root after independent audit; no prior057/Part11 waiver applies.

## Acceptance, wording and unfinished work

Focused unit/mounted/browser suites cover exact decimal/count rounding, zero/missing/overflow, source ownership and dates, current roles/tenant/CSRF/replay, changed plan/human basis, full ordinary paid/demo persistence and reset, keyboard/unit clearing/focus, long text and relevant error/paused states. No speculative broad historic rerun is implied. Seal exact loaded source/head/tree, raw SQL identities, outputs/screenshots and exploratory failures; different independent audit precedes release.

Affected rendered sinks: material disclosure, plan summary/current-or-older decision basis, source/date/availability statement, history, structured form, calculation/result, confirmation, cancel/save/withdraw, loading/validation/conflict/denied/limit/paused outcomes and accessible status. Native details/form controls and existing style classes cover desktop/mobile/light/dark; all new text is business-facing. A customer quote/export is not added. Part8 full professional paid/demo create-preview-download, truthful itemization/issuer/authentic footer requirements remain open.

Part2 still needs multiple materials, grounded source acquisition/freshness/availability, compatible richer unit/quantity/waste/cost evidence and deliberate adoption into a new canonical estimate revision with renewed human pricing. This plan is an attributed input, not completed Material Intelligence. HostedCI, historical3focusedAPI/15wider failures, physicalSafari/devices/manualAT, privateproduction/provider/storage, physicalrestore/RPO/RTO/laterautomaticbackup and founder personal visual verdict keep their prior limits.
