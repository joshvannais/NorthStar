# Unavailable and unclaimed evidence

- Exactly 24 `account-migration-010-postgres` cases remain unavailable because
  four `ACCOUNT_MIGRATION_*` disposable PostgreSQL URLs were absent. They were
  not fabricated and are not claimed passing.
- Optional security scanning remains unavailable because
  `CODEX_SECURITY_CONFIG_PATH` is unset. It was not invoked or waited on.
- Hosted checks are empty, not passing.
- Physical Safari and physical mobile devices are unavailable; actual
  Playwright WebKit 26.5 is not physical Safari.
- Provider accounts, credentials, live maps/calendar/fleet/telematics, provider
  calls, private production data, production migration, deployment, and passive
  production acceptance were not accessed or claimed.
- User visual approval remains separate and unclaimed.
