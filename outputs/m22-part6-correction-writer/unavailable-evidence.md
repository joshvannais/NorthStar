# Mission 22 Part 6 correction unavailable evidence

- The optional security scanner remains unavailable because its configuration
  is unset and its earlier invocation failed. This writer did not invoke,
  configure, request approval for, or wait on it. Manual complete-diff and
  mounted source-to-sink validation remains the applicable evidence.
- `tests/integration/account-migration-010-postgres.test.js` requires four
  disposable `ACCOUNT_MIGRATION_*` database URLs that are absent. Its 24 cases
  remain unavailable and are excluded from the locally available corpus; they
  are neither fabricated nor called passing.
- Hosted GitHub checks may remain absent. An empty checks list is unavailable,
  not passing.
- Physical Safari/device testing, provider accounts or calls, credentials,
  configuration, production data, production migration, Railway deployment,
  and production acceptance are outside this correction writer's authority and
  remain unclaimed. Actual Playwright WebKit is not physical Safari.
- User visual approval remains a separate gate and is not claimed by the
  screenshot package or automated browser matrices.
- The terminal Mission 22 lead, not this writer, must copy the released
  immutable employee-only screenshot package to the already verified canonical
  OneDrive evidence/screenshots destination and surface representative views in
  the master chat.
