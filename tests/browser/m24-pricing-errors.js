'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
process.env.NODE_ENV='test';process.env.AUTH_ACCESS_SECRET='pricing-browser-local-disposable-secret-at-least-thirty-two';for(const k of ['DATABASE_URL','MIGRATION_DATABASE_URL','OPENAI_API_KEY','RETELL_API_KEY','STRIPE_SECRET_KEY'])delete process.env[k];
const {createEstimateReviewFixture}=require('../helpers/m24-estimate-review-fixture'),{resolveBrowserRuntime}=require('../helpers/playwright-runtime');
const arg=n=>process.argv.find(v=>v.startsWith('--'+n+'=')).slice(n.length+3),engine=arg('browser'),out=path.resolve(arg('output'));assert.ok(!fs.existsSync(out));fs.mkdirSync(out,{recursive:true});
(async()=>{let f,server,browser,lastPage;const ledger={engine,cases:[],errors:[],pass:false};try{f=await createEstimateReviewFixture();server=f.app.listen(0,'127.0.0.1');await new Promise(r=>server.once('listening',r));const origin='http://127.0.0.1:'+server.address().port,rt=resolveBrowserRuntime(engine);browser=await rt.browserType.launch({headless:true,executablePath:rt.executablePath});
for(const demo of [false,true])for(const [theme,width]of[['light',1440],['dark',390]]){const tag=(demo?'demo':'paid')+'-'+theme+'-'+width,c=await browser.newContext({viewport:{width,height:1000},reducedMotion:'reduce'});await c.addInitScript(t=>localStorage.setItem('northstar-theme',t),theme);if(!demo)await c.addCookies(Object.entries(f.actors.owner.session.cookies).map(([name,value])=>({name,value,url:origin,sameSite:'Lax',httpOnly:name!=='northstar_csrf'})));const p=await c.newPage(),requests=[];lastPage=p;p.on('pageerror',e=>ledger.errors.push({tag,message:e.message}));p.on('request',r=>requests.push({path:new URL(r.url()).pathname,method:r.method()}));p.on('response',async r=>{if(/pricing-plan/.test(new URL(r.url()).pathname)&&r.request().method()==='POST')ledger.cases.push({tag:tag+'-response',path:new URL(r.url()).pathname,status:r.status(),body:await r.json().catch(()=>null)});});await p.goto(origin+(demo?'/demo':'/dashboard'));await p.waitForLoadState('networkidle');assert.ok(!/login|signin/.test(new URL(p.url()).pathname));if(await p.locator('#northstarQuickStartDialog[open]').count())await p.keyboard.press('Escape');const graph=demo?await p.evaluate(async()=>((await(await fetch('/api/demo/command-center')).json()).data.graphs[0])):f.estimateGraphs[width===390?1:0];if(demo)await p.getByRole('button',{name:graph.customer.name,exact:true}).first().click();else await p.evaluate(id=>CustomerDetail.open(id),graph.ids.customer);await p.locator('#cdPricingStart').waitFor({state:'attached'});await p.waitForFunction(()=>!document.querySelector('#cdEstimateReviewRefresh').disabled);
async function expand(id){for(const d of await p.locator('#'+id).locator('xpath=ancestor-or-self::details').all())if(await d.getAttribute('open')===null)await d.locator(':scope > summary').click();}

const reviewPath=(demo?'/api/demo/command-center/estimates/':'/api/v1/canonical/estimates/')+graph.ids.estimate+'/review',read=()=>p.evaluate(async route=>(await(await NorthStarAccountSession.fetch(route)).json()).data,reviewPath);const before=await read();assert.equal(requests.filter(r=>r.method==='POST').length,0);await expand('cdPricingStart');await p.locator('#cdPricingStart').click();await p.locator('#cdPricingLine-0-label').fill('Installation Package');await p.locator('#cdPricingLine-0-amount').fill('1500');await p.locator('#cdPricingReason').fill('Review proposed charge separately from approved price.');async function calculate(){await p.locator('#cdPricingCalculate').click();await p.waitForFunction(()=>document.querySelector('#cdPricingStatus').textContent.startsWith('Review the proposed amounts'));await p.locator('#cdPricingConfirm').check();}await calculate();await p.locator('#cdPricingLine-0-label').scrollIntoViewIfNeeded();await p.screenshot({path:path.join(out,tag+'-fields.png')});await p.locator('#cdPricingConfirm').scrollIntoViewIfNeeded();await p.screenshot({path:path.join(out,tag+'-review-visible.png')});const attempts=[];
await p.route('**/estimates/*/pricing-plans',async route=>{attempts.push({key:route.request().headers()['idempotency-key'],body:route.request().postData()});const response=await route.fetch();assert.equal(response.status(),201);await route.fulfill({status:503,contentType:'application/json',body:JSON.stringify({success:false,error:{message:'PRIVATE DIAGNOSTIC NOT FOR DISPLAY'}})});},{times:1});await p.locator('#cdPricingSave').click();await p.waitForFunction(()=>document.querySelector('#cdPricingStatus').textContent.includes('unconfirmed'));assert.equal(await p.locator('#cdPricingConfirm').isChecked(),true);assert.doesNotMatch(await p.locator('#cdPricingStatus').innerText(),/PRIVATE/);await p.screenshot({path:path.join(out,tag+'-uncertain.png')});await p.route('**/estimates/*/pricing-plans',async route=>{attempts.push({key:route.request().headers()['idempotency-key'],body:route.request().postData()});const response=await route.fetch();assert.equal(response.status(),200);assert.equal((await response.json()).data.replayed,true);await route.fulfill({response});},{times:1});await p.locator('#cdPricingSave').click();await p.locator('#cdPricingStart').waitFor({state:'attached'});assert.deepEqual(attempts[1],attempts[0]);let saved=await read();assert.equal(saved.pricingPlans.total,1);ledger.cases.push({tag,uncertainCommittedResponseRetainsExactRequest:true});
for(const [code,category,words]of[[404,'',/unavailable/],[413,'',/Shorten/],[429,'pricing_history_limit',/history limit/],[429,'',/limited/],[503,'pricing_paused',/paused/],[409,'',/changed/]]){await expand('cdPricingStart');await p.locator('#cdPricingStart').click();await p.locator('#cdPricingReason').fill('Review saved proposal.');await calculate();await p.route('**/estimates/*/pricing-plans',route=>route.fulfill({status:code,contentType:'application/json',body:JSON.stringify({success:false,error:{category,message:'PRIVATE DIAGNOSTIC NOT FOR DISPLAY'}})}),{times:1});await p.locator('#cdPricingSave').click();await p.waitForFunction(()=>!document.querySelector('#cdPricingSave').disabled);const wording=await p.locator('#cdPricingStatus').innerText();assert.match(wording,words);assert.doesNotMatch(wording,/PRIVATE/);assert.equal(await p.locator('#cdPricingConfirm').isChecked(),false);const posts=requests.filter(r=>r.method==='POST').length;await p.locator('#cdPricingSave').click();assert.equal(requests.filter(r=>r.method==='POST').length,posts);await p.locator('#cdPricingStatus').scrollIntoViewIfNeeded();await p.screenshot({path:path.join(out,tag+'-'+code+'-'+(category||'unknown')+'.png')});ledger.cases.push({tag,code,category,wording,invalidatedConsent:true,unchanged:(await read()).pricingPlans.current.digest===saved.pricingPlans.current.digest});await p.locator('#cdPricingCancel').click();assert.equal(await p.evaluate(()=>document.activeElement.id),'cdPricingStart');}
await p.goto(origin+(demo?'/demo/operations':'/dashboard/operations.html'));await p.waitForLoadState('networkidle');await p.goto(origin+(demo?'/demo':'/dashboard'));await p.waitForLoadState('networkidle');const after=await read();fs.writeFileSync(path.join(out,tag+'-before-after.json'),JSON.stringify({before,after,attempts},null,2));assert.deepEqual(after.pins,before.pins);assert.deepEqual(after.decisions,before.decisions);assert.equal(after.pricingPlans.total,1);if(demo)assert.deepEqual(requests.filter(r=>r.path.startsWith('/api/v1/')),[]);assert.ok(!/login|signin/.test(new URL(p.url()).pathname));await c.close();}
assert.deepEqual(ledger.errors,[]);ledger.pass=true;}catch(e){ledger.error={message:e.message,stack:e.stack};if(lastPage)await lastPage.screenshot({path:path.join(out,'failure.png')}).catch(()=>{});process.exitCode=1;}finally{if(browser)await browser.close();if(server)await new Promise(r=>server.close(r));if(f)await f.cleanup();fs.writeFileSync(path.join(out,'RESULT.json'),JSON.stringify(ledger,null,2));console.log(JSON.stringify({pass:ledger.pass,cases:ledger.cases.length,error:ledger.error?.message}));}})();
