# Mission 23 Part 12 — same-job acceptance candidate

This package adds a reproducible acceptance journey over the existing deployed
Mission 23 authorities. The founder subsequently required a wording review
between every step. This package therefore also corrects Mission 23 presentation
copy and rendered advice/handoff explanations. It adds no backend capability,
dependency, grant, migration, provider action, or Mission 24 implementation. Writer evidence is not
independent approval, release, or a claim that every roadmap residual is closed.

The starting release is `c7e011517f0a48c0d1fda0ba32d72871bbc7cc26` (Part 11,
PR #182). The coordinator's retained deployment receipt records sole automatic
deployment `12231628-9dd6-4eb1-9f0e-f006787e80f8`, healthy public acceptance and
54 matching applied migrations. This is supplied release evidence, not a new
production inspection by the Part 12 writer. The founder's explicit continuation
after disclosure accepts the existing untested-restore/forward-fix limitation;
the earlier premature-merge gate error remains recorded, not retroactively erased.

## Reproducible source and evidence

Run `tests/browser/m23-part12-acceptance.js --browser=chrome --output=NEW_PATH`
and separately with `--browser=webkit`. Each output must be a new directory.
Use the existing `M19_PG_ADMIN_URL`, `M19_EXPECTED_PG_DATA_DIR`,
`M19_EXPECTED_PG_PORT`, and `M19_TEST_RUN_ID` disposable-server identity gates.
They require loopback, the exact expected data directory and a non-5432 port.
The run asserts PostgreSQL 18.x, UTF8, UTC, locale C and checksums enabled.
Distinct nonsuperuser migration-owner and runtime roles are provisioned and
removed with the disposable suite database. The shared fixture's default behavior
is preserved; only the new opt-in path stops after real scheduling approvals.

Each browser run provisions one synthetic job and retains its single appointment,
assignment and execution IDs across all stages and viewport revisits. Separate
browser runs use separate disposable databases and naturally different UUIDs.
No route response is fulfilled by an interception or replaced with canned data.
Browser network interception only blocks external destinations and records
ordinary POST bodies. Provider transport is prohibited, not activated.

Only synthetic accounts, tenant/profile/workforce skill/location, lead graph and
explicitly labelled synthetic reviewed equipment research are seeded using the
fixture owner. Availability declarations, assignment/dispatch previews and
approvals, asset draft confirmation, and every operational action use mounted
production HTTP with ordinary durable sessions and CSRF. No assignment trigger
is disabled on this path. No availability, skill, research, physical stock,
qualification, safety or legal fact about an actual person or business is claimed.

## Requirement-to-evidence map

| Requirement | Executable evidence and boundary |
| --- | --- |
| Exact dispatch and execution | `m23-part12-journey.js` declares availability, previews/approves assignment and dispatch, then an assigned member initializes/starts the same execution through HTTP. The database asserts dispatched with no missing review authority. |
| Labor | Assigned-member manual operational interval, exact profile/time-zone/category pins and owner review. No payroll conclusion. |
| Materials | Explicit recorded adjustment and consumed seal, exact unit/location pins and owner review. Recorded movement is not proof of physical stock or cost. |
| Equipment | Synthetic reviewed exact configuration, human-confirmed asset draft, checkout/use reading/check-in against the same execution. No real research/provider verification. |
| Field evidence | Assigned-worker note, required ad-hoc checklist response and inspection observation. The exact checklist and inspection pins gate completion. Files remain unavailable, with no file requirement silently satisfied. |
| Progress and changes | Measured quantity and reported customer field-change facts; owner confirmation has no price or authorization-to-continue consequence. |
| Completion | Assigned member proposes exact evidence. Owner navigates from operational overview using keyboard, reviews the confirmation and explicitly approves in the actual browser. |
| Polaris | Mounted provider-free advisory projection pins nonempty labor/material/equipment/field-evidence/progress source sets, the exact execution digest, and grants no capabilities. |
| Downstream handoff | Actual browser consent saves one immutable internal Mission 24 reference receipt. All six source domains reconcile exactly to SQL IDs, revisions and digests; runtime has no direct table privilege. Delivery and consumption remain unavailable. |
| Reopening | Browser owner explicitly reopens; prior completion records remain byte-equivalent and original handoff source digest remains unchanged in SQL while its status becomes source-changed. |
| Resumption and worker continuity | Separate explicit browser resumption, then the assigned member records a follow-up note from the mobile worker page against the same job. |
| Responsive continuity | Same completed job revisited/reloaded in light/dark at 1440, 390 and 320 CSS pixels in installed Chrome and actual WebKit. Full-page screenshots plus mobile worker screenshot; no horizontal overflow. |
| Role/tenant boundary | Mounted current dispatcher, viewer and other-tenant owner requests cannot read the owner handoff. Broader security corpus remains separate. |
| Persistence and migration preservation | Exactly one execution remains. All 54 migration ledger entries survive runner reinitialization unchanged. No new migration; raw prior Git blobs must be sealed separately. |

The JSON ledger records requests and responses without cookie, CSRF, access-token,
connection-secret or provider-credential headers. It retains source provenance,
exact synthetic pins, browser version, cases, screenshots, and any failure.
Development attempts remain separate from the final immutable-head runs.

## Residuals and release gates

- The mandatory wording gate applies after every implementation/correction and
  before the next step. Review visible pages/cards/forms/dialogs, loading/empty/
  denied/error states, hints/tooltips and accessibility names across themes and
  device sizes. Replace engineering identifiers and implementation language with
  actionable business wording; preserve consent, permissions and consequences.
  The rendered wording assertion complements direct visual review; it does not
  claim exhaustive understanding of all business text. Internal diagnostic data,
  immutable source pins and backend validation remain intact.
- My Work Profile (`M23-01`) is historically released and production-accepted,
  not a missing Part 9 feature. The retained September 9 release acceptance
  records PR #180, approved head `619a4d7cc989f4fa480ef44efe6f1b2ead6a2b29`,
  merge `6b48d375c58f2bfd44bed83bdf5e920c89c9edf5`, and deployment
  `36582c99-7fab-45d1-8a50-b877516caf98`, and declares Part 9 complete.
  Current [My Work Profile](../M23_MY_WORK_PROFILE.md), migration 055 and mounted
  `/api/work-profiles` authority implement employee claims, owner/admin reviews,
  certification-reference decisions/expiry, self-declared availability and
  retained history. The frozen release receipt is copied into the corrected
  Part 12 evidence packet. These are historical source/release facts; this
  Part 12 same-job journey does not exercise the profile workflow or manufacture
  a new profile acceptance verdict. Final Part 12 release gates remain pending.
- Live file storage/scanning, provider activation, real research, private production
  rows, legal/professional approval, external delivery/consumption, physical
  Safari/devices and manual assistive technology remain unverified/unavailable.
- The inherited wider corpus result of 636/651, with 15 exact-base failures, is
  not rerun or reclassified as green by this focused acceptance package. Hosted
  CI and founder personal visual approval are separate evidence.
- The existing production backup receipt does not prove restoration. Only the
  reviewed forward-fix plan retaining migration 056 is the accepted continuation
  disposition; no destructive down-migration, restore, backup creation or new
  blanket recovery waiver is inferred.
- A different fresh auditor must review the exact immutable candidate. Root owns
  all remaining ordinary ready/merge, sole automatic deployment, exact revision,
  applicable migration verification, health, passive acceptance and final seal.
  The writer performs none of those production actions.
