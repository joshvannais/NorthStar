# Mission 25 Part 13A candidate evidence

- Base: `04f1bcd2b486e49fbfc0e04052c2170b634344f5`, independently accepted Part 12K and complete Part 12.
- Slice: A — canonical tenant-private cross-source job and outcome graph authority.
- Migration: `124_canonical_job_outcome_graph.sql`.
- The candidate adds no rendered path. API messages were reviewed for plain business language and capitalization.
- Fresh disposable PostgreSQL 17 applies the full migration chain through 124. The mounted Slice 13A journey creates exact current external project and financial outcomes, grants separate graph permission, builds the graph with concurrent deterministic replay, reads it, rejects duplicate nodes, denies member and other-tenant access, proves target and permission staleness, proves source regrant and graph regrant cannot revive prior evidence, verifies immutable storage and least-privilege entry functions, and confirms the canonical estimate digest is unchanged.
- Ten focused graph and accepted-outcome Jest contract/ratification suites passed 42 tests. Three accepted Node contract files passed six tests. These cover exact node and locator shapes, bounded graph size, distinct-source and distinct-domain requirements, consent confirmation, plain messages, migration pinning, route mounting and no rendered files.
- The mounted Slice 13A and accepted Part 12K API suites passed three tests against migration 124. Three unchanged Mission 24 estimate, labor and material suites passed 51 tests, and the accepted proposal-adoption Node contract passed four tests.
- Node syntax, migration timeout inclusion, `git diff --check` and the no-rendered-path review passed. A clean worktree is the final commit gate.

The legacy `m24-equipment-plans.test.js` suite was sampled as an unrelated broad check and retains three expectation failures at the immutable accepted base `04f1bcd2`; the same three failures reproduce without the Slice 13A diff. Slice 13A changes none of that suite's test, contract or demo-equipment paths, so it is recorded as existing baseline evidence rather than candidate evidence.

Slice A records lineage only. It does not evaluate cross-source conflict or completeness, summarize a job, generate a proposal, preview impact, adopt a change or propagate adoption. Those remain mandatory Slices B-H. Native NorthStar financial truth remains a Mission 27 dependency.

No live provider, provider credential, private production account, production data, production migration, deployment, physical-device review or manual assistive-technology review is claimed.
