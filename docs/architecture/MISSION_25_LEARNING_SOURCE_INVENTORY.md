# Mission 25 learning source inventory

This inventory distinguishes released source authority from future learning consumption. Table and route names identify implementation evidence; they do not grant learning permission.

| Source class | Released authority | What it may eventually support | Current Mission 25 disposition |
| --- | --- | --- | --- |
| Business profile and policies | Mission 20 organization profile, workforce, locations and assets | Baseline hours, rates, capacity, crew and asset policies | Source owner only; no automatic learning update. |
| Reviewed shared knowledge | Mission 21 registry/publication/provenance/sync | External capability and terminology context | Shared knowledge is not tenant outcome evidence. |
| Native customer and call graph | `canonical_customers`, transcripts, facts, communications, opportunities, appointments and voice sessions | Customer intent, requested scope, evidence provenance, lead disposition and call-to-job reconciliation | Transcript/fact confidence and exact source spans remain distinct from verified onsite or outcome facts; communications do not become satisfaction labels by default. |
| Schedule, assignment and dispatch | `canonical_schedule_assignments`, approvals, revisions, audit events and human approvals | Planned start/end, crew assignment, dispatch and schedule-versus-actual comparisons | Mission 22 retains authority; availability/conflict and route recommendation evidence must stay distinct from actual arrival, travel and work time. |
| Labor/time | `canonical_labor_intervals`, events and revisions | Planned-versus-actual labor and duration | First runtime source released for completed paid-tenant jobs: accepted non-break intervals may be compared with one adopted labor plan under current explicit purpose consent. Corrections stale the advice; revocation hides it. Worker privacy and employment conclusions remain bounded. |
| Materials/inventory | `canonical_material_movements`, events and revisions | Planned-versus-used quantity, waste and cost | Movements are evidence, not procurement price or stock valuation unless the exact source says so. |
| Equipment/vehicles | equipment asset versions, ledgers and events | Utilization, downtime, condition and operating variance | Part 10 Slice A consumes only complete effective checkout intervals for exact adopted, current reviewed native assets on completed jobs. It reports checkout-duration variance under separate purpose consent. It does not infer engine-on time, productive use, fuel, operating cost, maintenance, condition or availability. Shared equipment research remains separate. |
| Field progress/evidence | Mission 23 progress, field evidence and change facts | Scope change and execution-context variance | Preserve evidence type and source; never infer compliance or quality from activity alone. |
| Completion/reopening | `canonical_completion_records` and events | Outcome window and final operational state | Completion is not payment, profit, satisfaction or absence of later correction. |
| Downstream handoffs | `canonical_handoff_receipts` | Exact source-set discovery | Receipt is not learning access; current source permission and Mission 25 purpose consent are still required. |
| Estimate lineage and costs | canonical estimates, revisions, material/labor/equipment/travel/cost plans | Prediction baseline and cost-component variance | Historical estimates stay immutable; calibration produces a separate proposal. |
| Human price and commercial approval | estimate decisions, pricing policy, commercial terms/approvals | Approved scope/price/policy baseline | Human approval remains Mission 24 authority. |
| Issued estimate and customer response | customer estimate versions, delivery links/events | Exact quote accepted, questioned, revoked or expired | Acceptance is not job completion or collection. Questions are communications, not training labels by default. |
| Native invoice/payment/collection | Mission 27 future authority | Revenue, collections and realized margin evidence | Unavailable until Mission 27 supplies explicit compatible authority. |
| Compatibility `/polaris/learning` | Current Polaris snapshot projection | Read compatibility only | No outcome learning, training or calibration claim. |
| Demo workspace | Isolated fictional session state | Product demonstration | Never contributes to paid-tenant or aggregate learning. |
| External historical and continuous sources | `canonical_external_labor_import_consents`, `canonical_external_labor_import_runs`, `canonical_external_labor_import_records`, `canonical_external_labor_import_reference_matches`, `canonical_external_labor_import_learning_consents`, `canonical_external_labor_import_outcome_observations`, source-operation revisions and guarded external-labor routes | Older and ongoing CRM, fleet, accounting, payroll, fuel, routing, procurement and communications outcomes | Provider-neutral normalized labor/time envelope, reviewed worker/job reconciliation, separately consented imported labor-duration outcomes and multi-job calibration are released. The Owner Learning Center adds CSV backfill, provider-neutral adapter lifecycle, checkpoints, retention and bounded deletion cleanup. Corrections, tombstones, match changes and revocation stale or hide derived advice. Provider-specific adapters and other source classes remain unimplemented. |
| External travel evidence | `canonical_external_travel_import_consents`, `canonical_external_travel_import_runs`, `canonical_external_travel_import_records`, `canonical_external_travel_reference_matches`, `canonical_external_travel_import_learning_consents`, `canonical_external_travel_outcome_observations`, `canonical_external_travel_calibration_consents`, `canonical_external_travel_calibration_proposals`, travel-source operation revisions, `canonical_learning_center_read` and guarded external-travel routes | Route duration, mileage, fuel quantity and fuel cost estimate-versus-actual outcomes | Part 9 Slice A stages normalized, consented evidence with explicit units, currency, time zone and evidence class. Slice B explicitly reconciles opaque job and vehicle references to same-tenant estimates and current reviewed vehicle assets without guessing. Slice C separately consents and pins adopted-plan comparisons while leaving missing or incompatible dimensions unavailable. Slice D summarizes five to 100 current reviewed same-service outcomes with deterministic dimension-specific medians and quartiles. Slice E adds provider-neutral adapter lifecycle, checkpoints, retention and bounded deletion cleanup without storing provider credentials. Slice F exposes those guarded authorities through the paid owner Learning Center and an isolated read-only demo. Corrections and consent changes stale or hide derived advice. All six slices passed independent Part 9 acceptance. |
| External vehicle and equipment evidence | `canonical_external_asset_import_consents`, `canonical_external_asset_import_runs`, `canonical_external_asset_import_records`, `canonical_external_asset_reference_matches`, `canonical_external_asset_outcome_consents`, `canonical_external_asset_outcome_observations`, `canonical_external_asset_health_consents`, `canonical_external_asset_health_observations`, `canonical_external_asset_calibration_consents`, `canonical_external_asset_calibration_proposals`, asset-source operation revisions and guarded external-asset routes | Utilization, operating cost, maintenance and downtime outcomes | Part 10 Slices B-E stage normalized evidence, record explicit reviewed job and asset lineage, compare exact adopted plans and summarize exact-lineage health records. Slice F calibrates five to 100 fresh same-service utilization and operating-cost outcomes independently with deterministic medians and quartiles. Slice G adds provider-neutral lifecycle, checkpoints, retention and bounded deletion cleanup without credentials. Maintenance, downtime, condition and availability remain uncalibrated without an adopted comparison baseline. Corrections, tombstones, consent, reconciliation, plan, asset or ledger changes stale and mask advice. Nothing becomes a Mission 23 event or alters jobs, estimates, schedules, assets, costs, allocations or policy. Learning Center experience remains Slice H. |
| External material, inventory and purchasing evidence | `canonical_external_material_import_consents`, `canonical_external_material_import_runs`, `canonical_external_material_import_records`, `canonical_external_material_reference_matches`, `canonical_external_material_outcome_consents`, `canonical_external_material_quantity_observations`, `canonical_external_material_cost_consents`, `canonical_external_material_cost_observations`, `canonical_external_material_calibration_consents`, `canonical_external_material_calibration_proposals` and guarded external-material routes | Material use, waste, unit cost, purchasing, vendor lineage and timestamped inventory balances | Part 11 Slices B-E stage exact provider-neutral evidence, require explicit reviewed job/material/vendor/location links, and compare current imported records with exact adopted material plans under separate purpose consent. Slice F uses five to 100 fresh paired same-service D/E outcomes for independent equal-job-weight calibration of total use, waste, unit cost, purchased quantity and purchase cost. Exact units, plan shape and currency are required. Vendor lineage and balances are not numerically calibrated, and a balance never proves current availability. Corrections, tombstones, target, reconciliation, plan or consent changes stale and mask advice. Lifecycle and Learning Center work remain Slices G-H. |

## First runtime adoption criteria

The first runtime package should choose one narrow, already released, outcome-bearing source set with an exact business decision it can improve. It must include both its estimate/baseline side and actual/outcome side, current purpose consent, immutable observation lineage, correction/tombstone propagation, a deterministic advisory comparison and a human adoption boundary. It must not begin with a generic score, opaque model, dashboard-only mockup or cross-tenant corpus.

## Released labor-duration contract

Migration `083_canonical_labor_outcome_learning.sql` and `/api/v1/learning` implement that first narrow adoption. The baseline is the current adopted Mission 24 labor plan. The outcome is the complete set of current Mission 23 labor intervals for the linked completed execution; every non-break interval must be accepted, and breaks are retained in lineage but excluded from worker-hour totals. `canonical_labor_outcome_observations.source_manifest` pins every source identity, revision and digest used by algorithm `labor_duration_variance_v1`.

The comparison reports planned worker hours, recorded worker hours excluding breaks, absolute and percentage variance, and one deterministic advisory band. It is available only to current owners and administrators with active consent, and each observation retains an explicit confirmation and confirmation-contract version. A later source correction changes the live source digest and makes earlier advice unavailable until a new observation revision is recorded. Consent revocation prevents observation and suppresses derived history from runtime reads. A later re-grant can produce a new consent-bound observation from otherwise unchanged sources. No result is automatically adopted by estimating, scheduling, workforce or business-profile authority.

## Released external-labor import contract

Migration `084_canonical_external_labor_import_authority.sql` accepts only schema `m25-external-labor-time-v1`. A source is a tenant-owned opaque key rather than a vendor name baked into the schema. Historical pages and continuous updates use separate cursor chains. Batches contain at most 100 normalized records and preserve source record identity, version, source update time, work interval, category, opaque worker/job references, consent and immutable run lineage. Exact duplicate versions are counted without creating another record. A same-version payload conflict or older version fails closed. A newer version creates an immutable correction; a tombstone creates a detail-free current record.

The current projection is available only while that source's consent is active. It masks tombstoned work details and labels the records as staged evidence. No current record becomes a Mission 23 labor interval and no labor-duration advisory consumes it. Reviewed worker/job matching, retention/deletion execution, provider credentials, CSV/browser experience and source-specific adapters remain later work.

## Released external-labor reconciliation contract

Migration `085_canonical_external_labor_reconciliation.sql` provides reviewed worker and job reference reconciliation by recording explicit owner or administrator links from current external worker and job references to current same-tenant workforce profiles and estimates. The current imported-record manifest and target basis are digested independently. Corrections, tombstones, profile changes and membership status changes therefore make a prior link stale instead of silently changing its meaning. Consent revocation hides the projection and blocks writes. These links are advisory lineage only; no imported record is yet eligible for an outcome observation.

## Released imported labor-duration outcome contract

Migration `086_canonical_imported_labor_outcomes.sql` makes current matched external labor records eligible for one narrow planned-versus-recorded worker-hour comparison. The business must grant a separate purpose consent while source consent is current. The job match must point to the estimate and every current worker reference must have a current match. The immutable observation pins all source records and reviewed links. It reports a deterministic variance advisory and never adopts a changed rate, plan, price, schedule or policy.

## Released multi-job imported labor calibration contract

Migration `087_canonical_imported_labor_calibration.sql` adds separately consented, tenant-private multi-job imported labor calibration proposals. Five to 100 current reviewed estimate-and-external-job outcome chains for one normalized service key form a pinned sample. Freshness precedes the sample cap, and candidate evaluation fails closed beyond a documented 10,000-chain bound. The deterministic median and quartiles describe recorded-to-planned worker-hour ratios, while a fixed five-percent band produces an advisory only. Corrections or consent changes stale the proposal and mask its recommendation; delayed exact-key retries retain that masking. No operational authority is mutated.

## Released Owner Learning Center projection

Migration `088_canonical_learning_center.sql`, `/api/v1/learning/center`, and the paid and isolated-demo Learning Center routes give owners and administrators one bounded inventory of native comparison consent and external source/service identities. The paid interface composes existing guarded source-detail, consent, reference-match and calibration reads and mutations; it does not select protected tables or copy their data into a parallel browser authority. The demo uses explicit read-only fictional records. Every calibration remains advisory and every operational adoption remains outside this interface.

## Released import operations contract

Migration `089_canonical_external_labor_import_operations.sql` adds immutable adapter, retention, deletion and cleanup-run lineage. The Learning Center accepts an exact bounded CSV page through the existing import authority and exposes provider-neutral lifecycle and checkpoints without retaining provider credentials. Retention and deletion append minimized tombstones in resumable batches. A deletion request revokes source consent immediately; no later import or derived read remains available unless the owner cancels the request and separately grants new consent.

## Part 9 Slice A — normalized travel evidence

Migration `090_canonical_external_travel_import_authority.sql` accepts only `m25-external-travel-actual-v1`. Active records identify one opaque job and vehicle, exact route timestamps, an IANA time zone, evidence class and at least measured distance or fuel. Every present amount carries an explicit unit; fuel cost carries a currency or remains unavailable. This is staged source evidence. It is not a reconciled NorthStar job, asset, route, operating cost or estimate outcome until later reviewed authorities pin those links.

## Part 9 Slice B — reviewed travel reconciliation

Migration `091_canonical_external_travel_reconciliation.sql` records explicit owner or administrator links from current opaque travel references to a same-tenant canonical estimate or current active reviewed vehicle asset version. Each link pins the current consent, all current imported records that carry the reference and the target basis. Vehicle target lineage includes the reviewed asset identity plus its current equipment-ledger revision and digest when present. Source corrections, tombstones, asset or ledger changes and consent changes mark earlier links stale. Revocation hides the matching projection and candidate targets. The links remain lineage only and never rewrite either authority.

## Released Part 9 Slice C — imported travel outcomes

Migration `092_canonical_imported_travel_outcomes.sql` adds separate purpose consent and immutable observations for an exact adopted travel plan and the current reviewed imported records for one job. Every current vehicle reference and the job reference must retain a current owner-reviewed match. Duration, distance, fuel or energy quantity and fuel cost are compared independently. Miles, US gallons and kilowatt-hours retain explicit units; currency must match exactly. Missing data, straight-line geometry, incompatible energy classes and mixed or incomplete currencies remain unavailable. The observation pins the estimate, revision, travel plan, source consent, job match, vehicle matches and every current imported record. It is advisory and cannot alter operational or pricing authority.

## Released Part 9 Slice D — multi-job imported travel calibration

Migration `093_canonical_imported_travel_calibration.sql` adds separate calibration consent and immutable proposals for one exact normalized service key. Five to 100 current reviewed imported travel outcome chains form the pinned sample after freshness evaluation. Route duration, distance, fuel or energy quantity and fuel cost are summarized independently with deterministic median and lower and upper quartile actual-to-planned ratios. A dimension needs five compatible observations with one exact unit or currency. Incompatible dimensions remain unavailable without suppressing valid dimensions.

The median becomes an advisory multiplier and fixed five-percent bands describe whether the current assumption may be kept, increased or decreased. Source corrections, reviewed-match changes, adopted-plan changes, observation refreshes and consent changes make saved advice stale and mask the multiplier. Exact delayed request replay remains immutable. No estimate, travel plan, route, schedule, reimbursement, payroll record, asset, price or business policy is changed.

## Released Part 9 Slice E — travel-source operations

Migration `094_canonical_external_travel_import_operations.sql` adds immutable travel-source adapter, retention, deletion and cleanup-run lineage. Historical backfill and continuous-update checkpoints remain independent and visible through the guarded operations projection. Provider-neutral adapter state stores connection lifecycle and cadence only; provider credentials remain outside this authority.

Retention and deletion append minimized tombstones in bounded, resumable batches of one to 100 records. Each cleanup attempt persists an exact replay receipt, including a zero-record completion, and pins the current retention or deletion authority and its cursor. Changing the authority starts a new checkpoint chain. A deletion request appends source-consent revocation in the same serializable operation, which blocks later imports and hides current evidence. Cancellation does not restore consent; an owner or administrator must grant it separately. The runtime role can call only guarded entry functions and has no direct table or projection-helper access.

## Released Part 9 Slice F — owner Learning Center travel experience

Migration `095_canonical_learning_center_travel.sql` extends the bounded Learning Center inventory with an explicit `sourceKind` so an identical tenant label can identify separate labor and travel authorities without collision. The inventory is capped at 100 sources and 50 service groups per source. The paid page composes the existing guarded travel source, consent, reconciliation, operation and calibration routes; it never reads a protected travel table directly.

Travel calibration renders route duration, distance, fuel or energy quantity and fuel cost independently and preserves canonical unavailable reasons. A newly consented source with no deletion request is safely presented as not deleted. Inventory, source and service loads carry separate local generations; stale completions are ignored so a slow prior refresh or source cannot overwrite the newest inventory and selected source. The isolated demo contains fictional labor and travel sources, disables mutation controls and never contributes to paid-tenant learning. Page entry resets restored scroll position, loading exposes `aria-busy`, reference tables carry captions and column scopes, and compact travel grids collapse at the narrow breakpoint.

## Part 10 Slice A — native equipment checkout-duration contract

Migration `096_canonical_native_equipment_utilization.sql` adds purpose consent and immutable native equipment checkout-duration observations. The baseline is the planned hours in one explicitly adopted Mission 24 equipment-cost plan and its exact underlying equipment plan. The actual is the sum of complete effective `check_out` to `check_in` intervals recorded by Mission 23 for each exact current reviewed native asset on the linked completed execution. `use` events are retained in the lineage and must occur inside a checkout interval, but they do not create a separate duration claim.

Every observation pins the estimate, adopted revision, both equipment plans, execution, completion, exact asset versions and every effective event revision and digest. A corrected event changes the live source digest, so prior advice becomes stale and its recommendation is masked. Consent revocation blocks new observations and hides derived history. The output remains advisory and cannot mutate an estimate, price, schedule, asset, allocation or policy. Checkout duration is not engine-on time, productive utilization, fuel burn, operating cost, maintenance cost, condition or availability. External utilization, cost, maintenance and downtime evidence begins in Slice B; those claims remain unavailable in Slice A.

## Released Part 10 Slice C — reviewed external asset reconciliation

Migration `098_canonical_external_asset_reconciliation.sql` records explicit owner or administrator links from current opaque job, vehicle and equipment references to a same-tenant canonical estimate or current active reviewed asset version of the matching category. Each immutable link or unlink revision pins the exact current source consent, all current active imported records carrying that typed reference, and the current target basis. Vehicle and equipment targets include the reviewed asset identity, asset version and current equipment-ledger revision and digest when present.

No name, manufacturer, model or reference similarity creates a link. Mixed vehicle and equipment references remain separate typed review items. A source correction or tombstone, consent change, target removal, category/version/review change or equipment-ledger change makes the saved link stale. Revocation hides the reference projection and blocks writes; re-granting consent cannot revive an earlier link. These links provide lineage only. They do not change a job, estimate, schedule, vehicle, equipment record, cost, allocation or policy, and they do not create a Slice D observation.

## Released Part 10 Slice D — imported utilization and operating-cost observations

Migration `099_canonical_imported_asset_outcomes.sql` requires separate purpose consent before one reviewed external job can be compared with its exact adopted equipment and equipment-cost plans. Complete current job and asset reconciliations, current asset versions and equipment-ledger bases, normalized records, units, currency and deterministic calculation version are retained in immutable lineage.

The utilization dimension compares only recorded machine hours with planned equipment hours. Engine hours, mileage, kilometres, cycles and job counts remain unavailable because this package has no authority to convert them. The operating-cost dimension compares only current job operating evidence in the adopted plan currency with the plan's operating-cost component; no exchange rate or unrelated ownership charge is inferred. Each dimension remains independently available or unavailable and requires exact set equality between distinct reconciled target assets and adopted plan assets. Missing or extra targets and multiple external references linked to one target fail closed for that dimension. Source corrections, tombstones, plan or match changes, asset or ledger changes and consent changes stale and mask earlier advice. The package adds no rendered interface and no automatic operational mutation. Maintenance events, downtime, condition and availability remain outside Slice D.

## Released Part 10 Slice E — maintenance, downtime, condition and availability

Migration `100_canonical_imported_asset_health_outcomes.sql` consumes only current Slice B maintenance and downtime records after a current Slice C reviewed vehicle or equipment match and a separate Slice E learning consent. It pins current source permission, the complete current typed-reference source manifest, match revision and digest, current target asset version and equipment-ledger basis, every current health record, deterministic calculation version and request identity.

Maintenance and downtime are separate dimensions. Recorded status counts are factual summaries. Downtime duration is summarized only when current periods do not overlap. There is no source claim for current condition and no complete observation-window denominator for availability, so both remain explicitly unavailable. Corrections, tombstones, source or learning consent, match, asset-version and ledger changes invalidate prior advice. No provider credential, fuzzy reconciliation, automatic mutation, calibration or rendered surface is introduced.

## Part 10 Slice F candidate — multi-job vehicle and equipment calibration

Migration `101_canonical_imported_asset_calibration.sql` adds separately consented immutable proposals for five to 100 fresh same-service Slice D observations. Each sample pins current source and outcome consent, exact observation revisions and their complete adopted-plan, reviewed-match, target asset, equipment-ledger and imported-record source digests. Utilization and operating cost are summarized independently with median and lower and upper quartile actual-to-planned ratios; each dimension needs five compatible records with one exact unit or currency. Raw statistics remain intact, but only an inclusive `0.25` to `4.00` median can produce a one-step advisory multiplier. An outlier keeps its evidence and explicitly withholds advice.

Maintenance, downtime, condition and availability remain unavailable for calibration because the accepted sources do not provide an adopted comparison baseline. Source corrections, tombstones, match, plan, asset, ledger or consent changes make saved advice stale and mask its multiplier. Revocation hides and blocks derived work, while a new grant does not revive an old consent period. The candidate is API-only and advisory; it does not mutate any operational record or policy.

## Part 10 Slice G candidate — vehicle and equipment source operations

Migration `102_canonical_external_asset_import_operations.sql` adds immutable adapter, retention and deletion revisions plus bounded cleanup runs. Adapter records contain only a provider-neutral kind and cadence. They store no credentials, provider account identifier or claim of a live provider connection.

Retention and deletion append minimized source tombstones in batches of at most 100 and expose exact resumable checkpoints. Changing retention authority starts a new cleanup chain. A deletion request revokes current source permission immediately, blocking imports and derived reads before cleanup completes. A grant is rejected while deletion remains active; explicit cancellation and a new consent period are required for recovery. The new period cannot restore tombstoned evidence or revive prior reviewed matches, observations or calibration advice. Operations are owner or administrator controlled, tenant private, entry-function only and never mutate operational records or policy.

## Part 10 Slice H candidate — vehicle and equipment Learning Center

Migration `103_canonical_learning_center_assets.sql` adds asset sources and native equipment comparison consent to the bounded Learning Center projection. The paid owner and administrator page uses the existing guarded Part 10 routes for source state, consent, reviewed references, maintenance and downtime summaries, source operations and calibration. The isolated demo is fictional and read-only. Vehicle and equipment evidence stays separate from labor and travel even when a tenant uses the same source label.

The rendered experience identifies checkout duration as distinct from engine-on time, keeps unavailable dimensions explicit and never describes maintenance or downtime as proof of condition or availability. Active deletion presents a cancel-first recovery state; a new source-consent period cannot revive prior source detail or learning authority. No provider credential, live provider claim or operational mutation is added.

## Part 11 Slice F accepted — multi-job material and purchasing calibration

Migration `110_canonical_imported_material_calibration.sql` requires a distinct current calibration consent pinned to current Part 11D quantity and Part 11E cost consent. Five to 100 fresh paired observations for one normalized service form a deterministic sample. One current quantity/cost pair per exact canonical-estimate and imported-job identity contributes one job; distinct reviewed imported jobs linked to the same estimate remain distinct samples. Each job first contributes one median ratio for each complete comparable dimension, so plans with more lines do not receive more weight.

Total material use, waste, unit cost, purchased quantity and purchase cost are summarized independently with median and lower and upper quartiles. Exact units, plan shape and currency must match, and at least five current jobs are required per dimension. Raw statistics remain visible, while values outside the inclusive `0.25` to `4.00` range cannot produce an advisory multiplier. Vendor lineage and recorded balances remain outside calibration. Every result is advisory, immutable and consent-bound; corrections or lineage changes stale it, revocation hides it and later grants do not revive an earlier consent period.

## Part 11 Slice G candidate — material source lifecycle and cleanup

Material import operations retain only provider-neutral adapter kind and cadence. Immutable retention, deletion and legal/audit-hold revisions are tenant private. Cleanup is authorized by the exact current policy or deletion digest, processes at most 100 current source records, appends minimized tombstones and records a resumable immutable run. Active holds prevent cleanup. Deletion revokes source permission immediately; cancellation and a new consent period cannot revive prior source detail, reviewed links, observations or calibration.

## Part 12 Slice A candidate — staged CRM and field-service evidence

The accepted input is the exact provider-neutral schema `m25-external-crm-field-service-v1`. It stages five record classes only: customer, lead, job, appointment and issued estimate. The source envelope preserves an opaque source key, exact consent period, historical or continuous cursor chain, immutable run identity, record identity and source version, event and update times, IANA time zone, evidence class and provider evidence digest.

The record body retains opaque related references and a strict class-specific state. An explicit `unknown` remains unknown. Current active records are projected as `unmatched`; no name, address, contact detail or similar text creates a NorthStar relationship. Customer/job/estimate/execution/project/change-order/financial reconciliation belongs to Part 12E. Lead, appointment, issued-estimate and customer-response observations belong to Part 12F. External invoice, payment and accounting evidence belongs to Part 12D, while native NorthStar invoice and payment facts remain unavailable until Mission 27.

This schema provides no fields for provider credentials, provider account identity, customer names, email addresses, phone numbers, postal addresses, descriptions, notes, financial amounts or currency. Source adapters must supply opaque references rather than repurpose them for business details. The boundary cannot create or edit a NorthStar customer, lead, job, appointment, estimate, schedule, dispatch decision, invoice, payment or provider record. Provider-specific adapters, lifecycle operations, retention and deletion remain Part 12J.

## Part 12 Slice B candidate — staged project and change-order evidence

The accepted input is the exact provider-neutral schema `m25-external-project-change-order-v1`. It stages project and change-order records through separately granted, current-period permission. Both classes retain an opaque project reference; change orders additionally require their own opaque reference. Optional customer, job and estimate references remain unmatched until Part 12E reviewed reconciliation.

Projects preserve original-contract and current-contract values independently. Change orders preserve a separate change-order value and an explicit increase, decrease or no-change effect. Each dimension is either an exact nonnegative decimal string plus its recorded three-letter currency, or explicitly unavailable with no amount or currency. The authority does not calculate deltas, sum change orders, convert currencies, or infer current value from other records. Project and change-order lifecycle states remain separate, and `unknown` remains unknown.

Bounded historical and continuous pages preserve exact schema, source, permission-period, cursor, request, record-version, event-time, time-zone, evidence and correction lineage. Higher versions append corrections; tombstones retain only source identity, version, removal state and update time. Revocation masks the current period, and a later grant starts empty cursor and record lineage. Runtime receives four guarded entries and no storage or helper access. This slice does not connect a provider, reconcile references, calculate outcomes, or mutate a NorthStar customer, job, execution, estimate, project, change order, invoice, payment, schedule or policy. Later Parts 12C-K retain their frozen scope.

## Part 12 Slice C candidate — staged communication evidence

The accepted input is the exact provider-neutral schema `m25-external-communication-evidence-v1`. It stages communication, delivery and satisfaction records through a separate current-period permission. All three classes require one opaque communication reference and may retain opaque customer, lead, job, appointment, estimate and project references. Those references remain unmatched until Part 12E reviewed reconciliation.

Communication records preserve channel, direction and an intent claim independently. Intent is either explicitly unavailable or recorded as customer-explicit, human-reviewed or provider-classified evidence. Delivery records preserve only their exact source state. A delivery state never proves customer intent, satisfaction, estimate acceptance, work completion or payment. Satisfaction records accept only explicit customer feedback or human review of explicit customer feedback. Automated sentiment is not a satisfaction label.

The schema stores no customer name, contact detail, message body, subject or transcript. Every external identity and relationship reference must already be a source-scoped, non-reversible token represented as `ref_` plus 64 lowercase hexadecimal characters; cursors use `cur_` plus 64 lowercase hexadecimal characters. Node and PostgreSQL reject raw provider identifiers and content-bearing values instead of transforming or storing them. Claim objects have an exact three-field semantic shape, and the immutable record table accepts only time zones from the PostgreSQL 17 catalog captured by the unreleased authority migration. Bounded historical and continuous pages retain immutable source lineage, higher-version corrections and detail-free tombstones. Revocation masks the current period; regrant starts empty cursor and record lineage. Runtime receives only four guarded entries. This slice calls no provider, interprets no message content, calculates no outcome and changes no NorthStar customer, lead, job, appointment, estimate, project, communication, invoice, payment, schedule or policy. Parts 12D-K retain their frozen scope.

## Part 12 Slice D candidate — staged external financial evidence

The accepted input is the exact provider-neutral schema `m25-external-financial-evidence-v1`. It stages invoice, payment, collection and accounting-entry records under their own current-period permission. Optional customer, job, estimate, execution, project and change-order references remain opaque and unmatched until Part 12E. Each financial class requires its own exact opaque reference and state; one class cannot stand in for another.

Amount evidence has one exact four-field shape. It is either unavailable with no amount, currency or basis, or recorded as one bounded nonnegative decimal string, an uppercase three-letter currency and a class-specific basis. The importer does not derive balances, taxes, revenue, costs, profit, margin, payment status, collection status, accounting treatment or exchange rates. Currency is preserved exactly and never converted.

External references and cursors use the shared content-free token grammar. Stored time zones belong to the frozen PostgreSQL 17 catalog. Higher source versions append corrections, tombstones retain no financial or relationship detail, revocation hides current evidence, and a later grant starts empty lineage. Runtime receives only the four guarded entries. The authority stores no provider credentials, provider account identity, financial account or card details, customer contact data, memo, description or line-item text. It calls no provider, performs no reconciliation, and cannot change a NorthStar operating or financial record. Native NorthStar invoice, payment, collection and accounting authority remains unavailable until Mission 27.

## Part 12 Slice E candidate — reviewed business-system reconciliation

`canonical_external_business_reference_matches` records explicit, immutable owner or administrator decisions across the four accepted Part 12 source classes. Each decision is limited to the current source-permission period and pins the complete current source-record manifest for one exact opaque reference. Customer references can select a canonical customer; execution references can select a field execution; job, estimate, project, change-order and external financial references can select their exact owning canonical estimate. There is no native project, change-order, invoice, payment, collection or accounting target in this slice.

Each target basis pins only same-tenant canonical identity and revision evidence. Target lists expose bounded, safe and unique company labels while keeping opaque target identities as submission values. Ambiguous labels fail closed. Source corrections or tombstones, source-permission changes and target changes stale a link. Revocation hides all links for that permission period, and regranting source permission does not revive old source evidence or reconciliation. Runtime can execute only guarded read and mutation entries; tables and basis helpers remain withheld.

Reconciliation creates lineage for later Part 12 observations. It performs no fuzzy matching, provider call, outcome calculation or operational mutation. It cannot create or edit customers, jobs, estimates, executions, projects, change orders, invoices, payments, collections, accounting records, schedules, prices or company policy. Native financial authority remains a Mission 27 dependency.

## Part 12 Slice F candidate — reviewed customer outcome observations

The observation identity is one tenant, one CRM source, one communication source, one canonical estimate, one reviewed external CRM estimate reference and one reviewed external communication estimate reference. Separate purpose permission pins the current permission period of both sources. An observation then pins both reviewed-link revisions and digests, the current estimate target digest, complete relevant source manifests and the deterministic calculation version.

The CRM issued-estimate record provides the exact relationship anchor for its recorded lead and appointment references. Each of those three source states remains independent. Customer response uses only a uniquely latest current inbound customer-explicit or human-reviewed intent, or explicit satisfaction evidence, for the reviewed communication estimate reference. Delivery, outbound communication and provider-classified intent cannot establish that response. Missing, duplicate, conflicting or tied evidence stays unavailable rather than becoming a guessed state.

Source corrections, tombstones, permission changes, reviewed-link changes and target changes stale the observation. Revocation masks history, and later source permission does not revive an earlier purpose-permission period. The authority calls no provider, performs no fuzzy matching, and changes no operating record.

## Part 12 Slice G candidate — reviewed project outcomes

Migration `119_canonical_external_project_outcomes.sql` records separately consented immutable scope, change-order and project-delivery observations for one exact reviewed external project and same-tenant NorthStar estimate. It pins the current project source period, purpose permission, project match, every distinct current change-order match state, target basis, complete source manifest and deterministic request/calculation identity.

Contract claims, change-order evidence and delivery state remain separate. Contract movement and signed change-order value require exact same-currency evidence and are never treated as causally equivalent. Duplicate project or change-order records, unmatched change orders, mixed currency and incomplete intervals fail closed without suppressing another compatible dimension. Corrections or lineage changes stale and mask advice; revocation hides it and later grants revive nothing from an earlier period. The API-only result changes no operational or financial record.

## Part 12 Slice H candidate — reviewed external financial outcomes

Migration `120_canonical_external_financial_outcomes.sql` observes only current, explicitly reviewed external financial references for one same-tenant canonical estimate. It separately records explicit posted revenue, explicit collected amounts, explicit posted realized costs and a compatible same-currency margin. The immutable lineage pins the current source and purpose permissions, reviewed-reference set, exact source manifests, target digest, calculation version and request identity.

Invoice and payment states never become revenue or collection. Missing evidence never becomes zero. Duplicate, unknown, stale, non-posted, unavailable or mixed-currency evidence fails closed for the affected result. Native NorthStar financial records remain unavailable until Mission 27, and this authority changes no imported or operating record.

## Part 12 Slice I candidate — bounded business-system calibration

`canonical_external_business_calibration_consents` grants a distinct current-period purpose for one exact customer source pair, project source or financial source and pins the corresponding current Part 12F, G or H permission. `canonical_external_business_calibration_proposals` records a deterministic five-to-100 observation sample for one normalized service. The manifest retains exact current observation and source lineage; corrections, tombstones, reviewed-link changes, target changes and source or purpose permission changes make an earlier result stale.

Customer state distributions, project numeric and state summaries, and financial numeric summaries remain separate. A dimension needs at least five comparable current outcomes. Monetary dimensions require one exact currency, and no conversion is inferred. Every observation has equal sample weight. The records are immutable, tenant private and exposed only through guarded owner or administrator entries. They are advisory and cannot update an estimate, price, schedule, project, job, customer, financial record or policy. Provider adapters and credentials, Part 12J lifecycle authority, Part 12K rendered experience and Mission 27 native financial truth remain outside this slice.

## Part 12 Slice J candidate — external business source lifecycle and cleanup

`canonical_external_business_adapter_revisions`, retention revisions, deletion revisions, hold revisions and cleanup runs apply one provider-neutral lifecycle to the accepted CRM/field-service, project/change-order, communication and external financial source classes. Every change is immutable and tenant private. Cleanup adds minimized tombstones to the exact accepted source table in batches of no more than 100 records and records an exact resumable checkpoint.

Source permission, imports, holds, deletion and cleanup share one transaction lock for the same company, source class and source. An active legal or audit hold blocks cleanup without preventing an immediate deletion-triggered permission revocation. Active deletion blocks imports and a new permission grant. Cancellation followed by a new permission period starts empty and cannot revive old source records, reviewed links, outcomes or calibration. The lifecycle calls no provider, retains no provider credential and changes no operational or financial record. The rendered Learning Center and complete Part 12 acceptance remain Slice K; native NorthStar financial truth remains Mission 27.

Saved request results are replayable only while they remain current for that source and permission period. Revocation, deletion, cancellation, a new permission period or a later lifecycle or cleanup revision retires the old response; delayed retries are rejected without exposing the retired success payload.

## Part 13 Slice A candidate — canonical job outcome graph

`canonical_job_outcome_graphs` records a tenant-private lineage graph for one canonical estimate. A graph may contain only exact current observations from the accepted labor, travel, equipment, material, customer, project and available external financial authorities. Each node pins its observation identity, revision, digest, source digest and the bounded locator needed to re-read that exact authority. The graph also pins the current estimate target manifest and a separate job-outcome-graph permission period.

Every graph needs at least two distinct current sources across at least two outcome domains. Duplicate identities, stale observations, hidden source detail, mismatched estimates, unsupported locators and incomplete permission are rejected. The graph retains no outcome value and performs no conversion, conflict resolution, completeness assessment, summary, calibration, proposal or adoption. Those remain later Part 13 slices.

Corrections, tombstones, source or purpose permission changes, reviewed-link changes and estimate-target changes make an earlier graph stale and mask its nodes. Revoking graph permission hides all graph history from the current period. A later grant starts empty and cannot revive an earlier graph. Runtime access is limited to four guarded entry functions; storage and resolver helpers remain withheld.

## Part 12K presentation projection

`canonical_learning_center_read` version 5 adds only bounded source keys and normalized service keys for the four accepted business-system classes. Source detail, reviewed links, outcomes, calibration and lifecycle state continue through their existing guarded entries. The projection does not duplicate operational truth and cannot infer a source relationship, service, unit, currency, state or missing value.

The browser uses opaque target identity only as a submission value. It renders the canonical safe label supplied by the server, or withholds the choice. Customer comparisons and calibration require one unambiguous current CRM source and one unambiguous current communication source; a missing or ambiguous partner remains unavailable. Revocation and deletion masking are inherited from the accepted source, outcome, calibration and lifecycle authorities.
