'use strict';
const fs=require('node:fs'),path=require('node:path'),read=value=>fs.readFileSync(path.join(__dirname,'../..',value),'utf8');
describe('Mission 25 Part 11 Slice D imported material quantity outcomes',()=>{const migration=read('migrations/108_canonical_imported_material_quantity_outcomes.sql');
 test('pins exact current plan, permission, matches, bindings and imported records',()=>{for(const value of['canonical_external_material_quantity_observations','materialPlan','sourceConsent','jobMatch','materialMatches','bindings','importedRecords','canonical_external_material_reference_source_basis','canonical_external_material_reference_target_basis'])expect(migration).toContain(value);});
 test('keeps consumption and waste independent and missing evidence unavailable',()=>{for(const value of["'consumption'","'waste'","'totalQuantity'","'status','unavailable'",'Missing records are unavailable, not zero'])expect(migration).toContain(value);expect(migration).not.toMatch(/ILIKE|levenshtein|similarity|exchange.rate/i);});
 test('mounts guarded API only and withholds helper authority',()=>{const routes=read('src/routes/learning.js'),db=read('src/db.js');expect(routes).toContain("'/external-material-sources/:sourceKey/estimates/:estimateId/imported-material-quantity-outcomes'");for(const value of['REVOKE ALL PRIVILEGES ON TABLE public.canonical_external_material_outcome_consents','REVOKE ALL ON FUNCTION public.canonical_imported_material_outcome_basis','GRANT EXECUTE ON FUNCTION public.canonical_imported_material_quantity_read'])expect(db).toContain(value);expect(read('public/js/learning-center-page.js')).not.toContain('imported-material-quantity-outcomes');});
});
