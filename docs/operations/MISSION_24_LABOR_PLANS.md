# Mission24 Part3 Slice1 — task labor plans

Part3 has two planned slices. Slice1 adds saved labor planning; Slice2 owns adoption into canonical estimate revisions and selected Polaris/CAPELLA economics. Saving this plan does not change customer price, tax, material plans, wages, assignments or operational time. Original elapsed-hours calculations and all62 prior SQL files remain unchanged.

The shared owner/admin paid/demo review supports total worker-hours, people times elapsed hours, and quantity times explicitly declared worker-hours per supported unit. Base hourly cost plus explicit burden and all-in hourly cost are mutually exclusive. Unknown rates or burden leave the plan total unavailable; the known-cost subtotal is separately named. Exact integer/rational intermediates avoid rounding before each task cost, then whole-plan costs sum rounded cents. Supported currencies are USD/CAD/EUR. Inputs and bounds are in laborPlanContract.js; no researched productivity, wages or market rates are implied.

Quantity/productivity and cost/burden each carry a distinct declared source, reference, note, effective/end dates and geography. My Estimate does not need a fake date/reference. No end date means freshness unknown. Server UTC source assessment is returned for review and saved with acknowledgment/explanation; current cautions remain distinct from immutable saved assessment. Invalid dates, amount/unit/basis combinations, unsupported precision and currency mismatches cannot be overridden. Source strings remain raw in receipts. References are not fetched and do not prove provider verification.

The compact Labor Plan disclosure is a sibling of Material Basis inside the existing estimate review. Saved summary, source details, editing and history use ordinary plain-language controls, task-specific hours and cost units. Keyboard opening/cancel/save focus and invalidation are deliberate. Changing a basis/unit clears incompatible values; refreshing changed estimate/decision/plan pins clears prior confirmation and pending request. A genuinely uncertain save retains its exact request/key; known rejection clears confirmation for a fresh review. Existing paid scope/price authority is unchanged.

Migration065 creates the protected immutable canonical_labor_plans ledger and associated validation/read/mutation functions, and extends only the isolated demo mutation operation CHECK with labor_plan. The number was free at authoring. Paid writes require current owner/admin/session/tenant and present subscription authority, CSRF, SERIALIZABLE transaction, source/plan/global-decision pins and current expiry before replay and insertion after waits. Raw runtime table/helper access is withheld; only read/mutate entries are granted. Existing same-estimate row locking serializes labor saves with decisions/material/adoption. No mutable historical rows or duplicate graph identities are created.

Demo uses the same calculation/validation/projection and isolated history with an explicit twenty-event labor-plan limit. It retains current session/expiry checks, shared initialization, existing token/intent boundaries and deliberate reset consequences. Old sessions are not reset or silently supplied labor assumptions. New users practice actual calculate/save/revise/withdraw; no fake approval or paid authority. Internal labor_plan fits the existing16-character operation field without widening it.

## Recovery and release requirements

The new migration includes an ACCESS EXCLUSIVE CHECK change on demo_command_center_mutations; startup caps cover065 before the migration advisory wait, respecting tighter inherited limits. Actual populated upgrade/rollback/apply-once, contention and compatible pause/forward tests are required in the sealed packet. Source-controlled laborPlanPolicy.mutationsEnabled=false pauses new labor writes while preserving saved reads; combined decisionPolicy=false additionally pauses human decisions. Existing routes, fresh-read initialization and PR206 return fixes must be retained in all recovery builds. Old software that omits labor history is not a truthful recovery target after writes.

Exact candidate-derived pause identities and whether actually executed belong to the handoff. This document grants no production migration, backup, maintenance, merge/deploy or restoration permission. New DDL contention, unknown external writers/table sizes, online backup cutoff and physical restore limitations require concrete post-audit release readiness; prior release dispositions are consumed.

## Evidence and boundaries

Focused tests cover arithmetic/rounding/missingness, JS/SQL source assessment, current access and concurrent saves, immutable histories and source binding, ordinary paid/demo browser tasks and fresh required reads, actual pause/forward recovery and bounded startup failure. Every failure remains in the packet with source attribution. Browser engines do not establish physical Safari/device or live provider evidence. Original financial/operational snapshots, material and human decision histories are checked unchanged. No broad previous corpus was repeated. Full Part3 acceptance follows Slice2 and independent review; no completion is claimed here.

## Reviewed save failures

An ambiguous HTTP503 or network failure does not prove that a save rolled back. The drawer retains its identical key, body, calculation and confirmation so an unchanged retry can recover the receipt. Only the allowlisted labor_paused response category paired with503 proves the explicit policy rejection; it clears confirmation and directs the user to saved history. This category is emitted only for LABOR_PLAN_PAUSED by the two labor-save routes. Unknown429 responses use neutral wait/history guidance; they do not assert a twenty-plan limit or prescribe reset. Known rejection still invalidates the attempted review. No authority, replay, ledger or migration rule changed in this correction.
