# Mission24 Part2 Slice7 — Material Integration And Acceptance

This is the seventh and final planned Part2 slice. Candidate verification, independent review and release acceptance are separate gates; this document does not declare Part2 complete. The separate owner Operations demo delivery remains after Part2 acceptance and before Part3.

## Released basis and bounded change

Base 59647039c4e9179b3616684e05c47b57dfb46e7f contains immutable multi-material plans, source declarations, dated availability evidence, alternatives, whole-plan estimate adoption and revision-specific human decisions. Slice7 fixes CAPELLA's omitted v4 adoption version and adds a private Saved Material Review to selected Polaris records using the existing authorized review. It never changes original prices, taxes, operational graphs or supplier records.

CAPELLA now obtains supported versions from [the adoption contract](../../src/estimating/materialAdoptionContract.js). A present unsupported child fails unavailable; it cannot fall back to original costs. A matching renewed human decision is still required. Exact string cents remain exact; missing applicable costs remain unavailable. For an illustrative original direct cost of650, selected direct cost183 and human price700, the remaining amount is517, not50. This arithmetic is not a guarantee of market price or profit.

## Consumer ownership

| Consumer | Basis and behavior |
| --- | --- |
| Shared customer drawer, material review and CAPELLA | Selected estimate revision; included immutable plan stays separate from a newer saved or withdrawn plan; fresh matching human decision required. |
| Selected Polaris Saved Material Review and explicit local material query | Existing owner/admin estimate-review endpoint, shared paid/demo projector and renderer; current estimate by default, explicit history selection, refresh without writes. Latest saved plan is separately labeled. |
| Polaris general provider conversation | Existing strict original context protocol remains unchanged. No new material receipts or private source evidence is sent to a provider. Saved Material Review states this distinction. |
| Demo profit/material questions | Same authorized isolated review; no blanket assertion that all material/labor costs are absent. No selected record means selection guidance. |
| Leads, standalone lead, Communications, Calendar, Command Center and original financial/analytics summaries | Original estimate/price guidance remains original; no revised economics or human price aggregation is implied. Current detailed costs are reviewed through the selected drawer/Polaris path. |
| Workers, Operations, future quotes/invoices | No new private economics or price authority. Operations demo is separately assigned; quote output remains later Part8. |

## Current authority, consistency and privacy

The existing paid context still requires current access and conversation entitlement. The material read resolves the exact selected graph and estimate, then the existing read transaction enforces current owner/admin membership and tenant. Demo resolves only its own selected graph and uses its existing isolated review adapter. Unknown selection never substitutes the first record. Each request captures selection and generation; a later selection or refresh suppresses stale responses. History remains explicit, source pins remain internal, and the read never changes consent or prices.

The shared display uses returned totals and assessments without another calculator. Included costs never silently become the latest unadopted plan. Source/availability cautions describe human-recorded evidence and missing or expired information; sufficient reported quantity is not reserved stock. UTC dates describe recorded evidence, not current supplier verification. The UI exposes no IDs, digests or raw failure diagnostics.

## Part2 exit evidence

Required acceptance covers renewed v4 approval and CAPELLA consistency, current/historical included versus later-plan basis, exact quantities/waste/whole-item rounding/cents across supported independent currencies, source and availability unknowns and retained alternative lineage, current access, unchanged history, shared paid/demo interaction, keyboard/mobile/themes and read-preserving pause/forward resume. Tests must distinguish local fixtures from live evidence and preserve failed exploratory attempts. The package handoff maps exact tested source and prior evidence; no broad historical rerun is implied.

Human-entered sources and reported availability are the delivered bounded capability. Live supplier verification, inventory reservation/purchasing and provider activation are unavailable, not passing. Later labor/equipment/travel intelligence, pricing, general reasoning/Risk Lens sensitivity, customer quote creation and whole-mission validation retain their original Part ownership.

## Recovery and release

No schema, migration, startup, operation type or provider protocol changes. All61 applied SQL identities remain unchanged. Current material/adoption pause and combined decision pause must preserve this corrected reader and populated history; prepared and actually executed builds are identified separately in the handoff. Rolling back to an older CAPELLA reader would restore the v4 display defect. No production stop, backup, deployment or provider action is authorized by this implementation package. Physical restore and external provider evidence remain unavailable.
