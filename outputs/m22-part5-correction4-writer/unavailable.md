# Unavailable and unclaimed

- Exactly 24 account-migration cases require absent disposable PostgreSQL URL
  groups. They were not fabricated or called passing.
- Optional security scanner configuration is unset; no scan/retry/wait occurred.
- Hosted checks are empty, not passing.
- Physical Safari/devices are unavailable; WebKit is not physical Safari.
- Providers/credentials/config, production data/migration/deployment/acceptance,
  and user visual approval were not accessed or claimed.
