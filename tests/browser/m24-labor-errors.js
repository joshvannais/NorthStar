'use strict';
const assert=require('node:assert/strict'),fs=require('fs'),path=require('path');process.env.NODE_ENV='test';process.env.AUTH_ACCESS_SECRET='m24-labor-browser-local-disposable-secret';for(const k of ['DATABASE_URL','MIGRATION_DATABASE_URL','OPENAI_API_KEY','RETELL_API_KEY','STRIPE_SECRET_KEY'])delete process.env[k];const{createEstimateReviewFixture}=require('../helpers/m24-estimate-review-fixture'),{resolveBrowserRuntime}=require('../helpers/playwright-runtime');const arg=n=>process.argv.find(v=>v.startsWith('--'+n+'=')).slice(n.length+3),engine=arg('browser'),out=path.resolve(arg('output'));assert.ok(!fs.existsSync(out));fs.mkdirSync(out,{recursive:true});
(async()=>{let f,server,browser;const ledger={engine,cases:[],errors:[]};try{f=await createEstimateReviewFixture({recordedLaborHours:2});server=f.app.listen(0,'127.0.0.1');await new Promise(r=>server.once('listening',r));const origin='http://127.0.0.1:'+server.address().port,rt=resolveBrowserRuntime(engine);browser=await rt.browserType.launch({headless:true,executablePath:rt.executablePath});
for(const demo of (process.argv.includes('--demo-only')?[true]:[false,true]))for(const[theme,width]of [['light',1440],['dark',390]]){const tag=(demo?'demo':'paid')+'-'+theme+'-'+width,c=await browser.newContext({viewport:{width,height:1000},reducedMotion:'reduce'});await c.addInitScript(t=>localStorage.setItem('northstar-theme',t),theme);if(!demo)await c.addCookies(Object.entries(f.actors.owner.session.cookies).map(([name,value])=>({name,value,url:origin,sameSite:'Lax',httpOnly:name!=='northstar_csrf'})));const p=await c.newPage();const calls=[];p.on('pageerror',e=>ledger.errors.push(e.message));p.on('request',r=>calls.push({path:new URL(r.url()).pathname,method:r.method()}));await p.goto(origin+(demo?'/demo':'/dashboard'));await p.waitForLoadState('networkidle');if(await p.locator('#northstarQuickStartDialog[open]').count())await p.keyboard.press('Escape');const graph=demo?await p.evaluate(async()=>((await(await fetch('/api/demo/command-center')).json()).data.graphs[0])):f.estimateGraphs[0];
if(demo)await p.getByRole('button',{name:graph.customer.name,exact:true}).first().click();else await p.evaluate(id=>CustomerDetail.open(id),graph.ids.customer);await p.locator('#cdLaborPlan').waitFor({state:'attached'});await p.waitForFunction(()=>!document.querySelector('#cdEstimateReviewRefresh').disabled);
async function expand(){const selectors=await p.locator('#cdLaborPlan').evaluate(e=>{const result=[];for(let x=e;x;x=x.parentElement)if(x.tagName==='DETAILS'&&!x.open)result.unshift(x.id?'#'+x.id:'.'+x.classList[0]);return result;});for(const s of selectors)await p.locator(s+' > summary').first().click();}
async function click(name){await p.getByRole('button',{name,exact:true}).focus();await p.keyboard.press('Enter');}
await expand();assert.equal(calls.filter(r=>r.method==='POST').length,0,'fresh page readiness must not require writes');await click(await p.locator('#cdLaborStart').innerText());assert.equal(await p.evaluate(()=>document.activeElement.id),'cdLaborTask-0');await p.locator('#cdLaborTask-0').fill((graph.lead&&graph.lead.serviceLabel||'Installation')+' Work');await p.locator('#cdLaborBasis-0').selectOption('people_time');await p.locator('#cdLaborPeople-0').fill('2');await p.locator('#cdLaborElapsed-0').fill('8');await p.locator('#cdLaborRate-0').fill('50.00');await p.locator('#cdLaborBurden-0').fill('20');await p.locator('#cdLaborReason').fill('Plan the on-site installation');const beforeUnchecked=calls.filter(r=>r.method==='POST').length;await click('Save Labor Plan');assert.equal(calls.filter(r=>r.method==='POST').length,beforeUnchecked);assert.match(await p.locator('#cdLaborStatus').innerText(),/Calculate/);await click('Calculate Labor Cost');await p.waitForFunction(()=>document.querySelector('#cdLaborResult').textContent.includes('960'));await p.locator('#cdLaborExplanation').fill('My task-specific estimate; dates and rates have not been independently verified.');await p.locator('#cdLaborConfirm').focus();await p.keyboard.press('Space');assert.equal(await p.locator('#cdLaborConfirm').isChecked(),true);await p.locator('#cdLaborTask-0').scrollIntoViewIfNeeded();await p.screenshot({path:path.join(out,tag+'-task-fields.png')});await p.locator('#cdLaborConfirm').scrollIntoViewIfNeeded();assert.equal(await p.locator('#cdLaborPlan').evaluate(e=>/[\u00c2\ufffd]/.test(e.innerText)),false);await p.screenshot({path:path.join(out,tag+'-expanded-plan.png')});const attempts=[];
await p.route('**/estimates/*/labor-plans',async route=>{attempts.push({key:route.request().headers()['idempotency-key'],body:route.request().postData()});const response=await route.fetch();assert.equal(response.status(),201);await route.fulfill({status:503,contentType:'application/json',body:JSON.stringify({success:false,error:{message:'PRIVATE DIAGNOSTIC NOT FOR DISPLAY'}})});},{times:1});
await click('Save Labor Plan');await p.waitForFunction(()=>document.querySelector('#cdLaborStatus').textContent.includes('unconfirmed'));assert.equal(await p.locator('#cdLaborConfirm').isChecked(),true);assert.match(await p.locator('#cdLaborResult').innerText(),/960/);assert.doesNotMatch(await p.locator('#cdLaborStatus').innerText(),/PRIVATE/);await p.getByRole('button',{name:'Save Labor Plan',exact:true}).waitFor();await p.waitForFunction(()=>!Array.from(document.querySelectorAll('#cdLaborPlan button')).find(b=>b.textContent==='Save Labor Plan').disabled);await p.screenshot({path:path.join(out,tag+'-uncertain-save.png')});
await p.route('**/estimates/*/labor-plans',async route=>{attempts.push({key:route.request().headers()['idempotency-key'],body:route.request().postData()});const response=await route.fetch();assert.equal(response.status(),200);assert.equal((await response.json()).data.replayed,true);await route.fulfill({response});},{times:1});await click('Save Labor Plan');await p.locator('#cdLaborStart').waitFor();assert.deepEqual(attempts[1],attempts[0]);await expand();assert.match(await p.locator('#cdLaborPlan').innerText(),/960/);
for(const category of ['unknown','temporary','session','capacity','paused']){
 await click(await p.locator('#cdLaborStart').innerText());await p.locator('#cdLaborReason').fill('Review saved labor');await click('Calculate Labor Cost');await p.waitForFunction(()=>document.querySelector('#cdLaborResult').textContent.includes('960'));await p.locator('#cdLaborExplanation').fill('Retain my declared assumptions.');await p.locator('#cdLaborConfirm').check();
 await p.route('**/estimates/*/labor-plans',route=>route.fulfill({status:category==='paused'?503:429,contentType:'application/json',body:JSON.stringify({success:false,error:{category:category==='paused'?'labor_paused':category,message:'PRIVATE DIAGNOSTIC NOT FOR DISPLAY'}})}),{times:1});
 await click('Save Labor Plan');await p.waitForFunction(()=>/limited|paused/.test(document.querySelector('#cdLaborStatus').textContent));const wording=await p.locator('#cdLaborStatus').innerText();assert.doesNotMatch(wording,/PRIVATE|20|reset|labor-plan limit/i);assert.match(wording,category==='paused'?/^New labor plans are paused\. Refresh to check saved history\.$/:/Wait before trying again/);assert.equal(await p.locator('#cdLaborConfirm').isChecked(),false);assert.equal(await p.locator('#cdLaborResult').innerText(),'');await p.screenshot({path:path.join(out,tag+'-'+category+'.png')});await click('Cancel Labor Plan');assert.match(await p.locator('#cdLaborPlan').innerText(),/960/);
}
if(demo)assert.equal(calls.filter(r=>r.path.startsWith('/api/v1/')).length,0);
ledger.cases.push({tag,passed:true,committed503IdenticalReplay:true,rejectionPartitions:['unknown429','temporary429','session429','capacity429','knownPaused503'],attempts,privateCalls:calls.filter(r=>r.path.startsWith('/api/v1/')).length});await c.close();}
assert.deepEqual(ledger.errors,[]);ledger.pass=true;
}catch(e){ledger.error=e.stack;process.exitCode=1;}finally{if(browser)await browser.close();if(server)await new Promise(r=>server.close(r));if(f)await f.cleanup();fs.writeFileSync(path.join(out,'ledger.json'),JSON.stringify(ledger,null,2));console.log(JSON.stringify(ledger));}})();

