# Mission 25 Part 14B acceptance evidence

Candidate scope: complete resettable fictional demo journey with strict paid/demo isolation.

The account-free Learning Center reads a session-private PostgreSQL projection. Its completed-job journey starts with explicit fictional permissions and supports reviewed cohort selection, proposal preparation, evidence and uncertainty review, saved review, owner adoption, removal and deliberate reset. The exact five selected jobs are validated against the current fictional workspace, pinned into the fictional proposal evidence and shown as a five-of-five sample; the three jobs left out are not silently counted. Every step uses the existing bounded demo revision, idempotency, expiry, action-limit and same-origin controls. A reset clears the journey by creating a fresh deterministic fictional workspace.

The new `learning_step` operation is limited by the demo mutation table constraint and changes only isolated demo state and its bounded audit trail. Mounted PostgreSQL evidence hashes every non-demo public table before and after the complete journey and reset, excluding only request audit records that are expected to record API access. It proves separate browser cookies do not share state, rejects stale revisions and foreign origins, and rejects an invented direct-table operation. Missing material, scope and financial evidence remains unavailable and is not treated as zero.

Fresh PostgreSQL 17 runs apply migrations 001–133. The focused Slice B unit, mounted API and ratification set is 3 suites / 9 tests; the protected migration checksum set adds 1 suite / 36 tests. Mounted controls prove that four and six through eight selections fail closed, while exactly five current unique fictional jobs succeeds; duplicate and foreign selections also fail closed. The accepted paid-journey compatibility set is 2 suites / 6 tests. Chrome 153.0.8010.48 and Playwright WebKit 26.5 each complete all five layouts with no page errors or outside requests. Final browser ledgers are `chrome-selected-ledger.json` and `webkit-selected-ledger.json`; state captures are sealed beside them under `C:\Users\joshv\Documents\Codex\2026-09-18\m25-part14b-evidence`.

Rendered evidence covers Chrome and Playwright WebKit at 360×800, 390×844, 768×1024, 1024×768 and 1440×900 in light and dark themes. It covers first use, permission grant, explicit cohort selection, review, save, adoption, removal, reset, reload-at-top, keyboard labels, overflow, page errors, outside requests and visible identifier or internal-language leakage. Physical Safari/device and manual assistive-technology evidence remain unavailable; the founder's visual verdict remains separate.

No provider, credential, private production, merge, deployment or release action is part of this candidate.
