# Pinned Reviewed PDF Text - Candidate Evidence Only

This is a reduced, pinned-document capability, NOT general autonomous PDF extraction. Runtime PDF parsing/worker and pdfjs/native dependencies are removed. Package files exactly match current released main. No V8 heap limit, empty environment, or worker termination is claimed as total-memory isolation.

Reviewed source-controlled entries explicitly set documentType=pdf plus reviewedExtraction. Exact fields: version reviewed-pinned-text-v1, rawBytes, rawDocumentDigest, text, textDigest, pages, reviewedBy, reviewedOn, sourceExtractionVersion. A separate reviewer must verify complete relevant text against the actual document, exact raw/text hashes, page count, reading order, tables and context. Page count and extraction provenance are declarations established by review; runtime does not parse or verify them. Review cannot postdate the registry access review. Registry hash binds the entire artifact and existing before/after current-source guards remain.

Acquisition downloads at most262144 raw bytes under existing public DNS/address/origin/redirect and whole8second deadline. It matches the exact raw length/SHA256 and returns only the matching pre-reviewed text. Any change fails closed with Review its contents before using it; no stale text reuse, decompression or fallback parser. Limits remain8reviewed pages,32768UTF8 text bytes/document,8documents,60000combined candidate bytes. Text/hash/metadata validation is bounded source-configuration checking, not a general parser sandbox. Raw response/request close is awaited on success/rejection/redirect/timeout before return or next download (F2).

Legacy text candidates keep their exact v1 shapes/digests; PDFs add existing tax-document-provenance-v1 with extractionVersion reviewed-pinned-text-v1. This identifies the artifact matching mechanism, not a parser execution. Raw/extracted hashes, size/pages/finalURL remain. Production factory adds full registry digest and access review. No validation, rate, effective-end-date, safety or legal authority inferred. All72SQL unchanged; no new admission/privilege/version changes. Package B remains unimplemented.

Production registry stays EMPTY/default-off: no source is activated by this patch, and no actual Windsor tax coverage is published. Existing official fixture remains historical evidence only: CT PDF19395bytes SHA25643d26dd3f75242c6d37c6a5acd1874fd19975fd3dee8c3fdb730a4bb260ca4cc, fetched September13,2026. Prior parser output4pages/7839bytes is predecessor-only evidence, NOT a newly independently reviewed/registered artifact. Focused successor tests use explicitly synthetic opaque bytes/text to prove matching and candidate isolation without parsing. A real source entry requires independent document/text review and separate activation disposition. No outreach/network/provider calls in these tests.

General extraction remains unsupported until genuine contained infrastructure is established. Offline review of new untrusted documents is not claimed safe merely because a worker or this thread exists. No new infrastructure requested or created.

No UI change. Existing candidate/review-required status remains; failures are handled by existing research worker, no raw diagnostics exposed. Source-only rendered mapping, not new browser proof. No schema/recovery rerun warranted; no migration/startup/permissions delta. Both operational checkpoints remain OPEN. Automatic proposal package sequence and future taxB require their separate dispositions.
