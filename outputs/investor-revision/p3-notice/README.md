# P3 cash-basis explanatory notice correction

Prior reviewed head: `90bd1de43c139e6fc903843726a17ce459b68fba`. The same reviewer's terminal result closed P1/P2 and identified one P3: an explicit total-bank → legacy-usable transition retained the old no-offset sentence, despite correct numerical balances and row provenance. Root required that residual corrected before release.

The notice now regenerates from the current declared basis and current restricted-funds amount every time actual-history configuration is built. Saved explanatory copy is never authoritative. Legacy normalization remains versioned v1; original actual facts remain unchanged. No cash formula, cash-basis selection rule, header, layout, CSS or UI script changed.

Tests first: the prior 14 focused tests passed and three added notice tests failed on the old head. After correction, 17 focused tests pass, including both transition directions, repeated serialized reloads, stale-text replacement and restriction changes through $1,000, $5,000, $0 and $2,000. A legacy $24,000/$23,700 input with a $1,000 offset still yields $25,000/$24,700 bank balances; $20,000/$19,700 with a $5,000 offset yields the same bank balances. This is normalization, not financing or a payment.

## Verification on this revision

`node scripts/verify-investor-notice.cjs` completed 4/4 immutable-source jobs on HTML SHA256 `4603c93fb9cd624bf7ca3748fe2d064f30104cc57fca4548ccd741250b3b266e`:

- 17 focused actual-history tests.
- All 51 original numerical tests, including worker-source parity and seeded production-engine simulation; 57 original receipts and 904 independently reconciled forecast rows.
- 8 route/hosting/source-contract tests.
- 58 rendered-meaning browser assertions across 1440px desktop and 390px mobile, including both notice directions, actual save/reset/load, changed offset, raw facts, known costs/payroll/receipts, and monthly/annual exports.

The scope check proves the numerical engine outside `createDefaultConfig`, the UI script, all CSS and header markup are byte-identical to 90bd1de. The embedded worker was mechanically resynchronized with the canonical engine. Four new notice screenshots are in `../screenshots/notice-{1440,390}-{legacy,total-bank}.png`; original actual previews were regenerated by the relevant browser suite. Writer inspected desktop/mobile notice renders; this is agent inspection, not founder inspection.

## Earlier evidence retained, not rerun here

The 276-control binding campaign and original four-case 2,544-assertion broad browser suite remain verified at **90bd1de**, HTML SHA `ced7943c914c1f4c28f4c21d2bb27fd1d464047012866df7bf2ff74bbd96477b`. They were not rerun solely for this copy correction. The parent-directory `verification-results.json`, broad browser/binding outputs and their earlier logs are historical receipts for that head. This directory's `verification-results.json` and four logs control this new focused verification; do not combine their provenance.

The previous independent review artifacts are unchanged: all 35 original-manifest and 41 correction-manifest artifact hashes were rechecked with zero mismatches. Existing financial, tax, legal, staffing, provider and physical-device caveats remain. No push, deployment, provider call, real payment or legal-document mutation was performed. Stop at the additive local freeze for the same reviewer's exact-diff check; root owns release coordination.
