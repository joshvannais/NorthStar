# Unavailable and non-passing evidence

This is a writer candidate. Independent exact-head audit has not happened.
There is no authority here to merge, deploy or begin Mission 23 Part 6.

- Hosted CI is unavailable in this writer lane; local results are not hosted CI.
- Physical Safari, physical mobile/tablet devices, and an actual screen-reader
  hardware/software session are unavailable. Actual Playwright WebKit, native
  keyboard/disclosure semantics, touch emulation and geometry are separately
  measured. CSS zoom and narrow-viewport reflow do not claim native browser zoom.
- Founder personal visual approval is unavailable. Writer screenshot inspection
  is not the founder's verdict or independent product approval.
- Live OpenAI/research/provider calls, provider enablement, provider configuration
  changes and new credentials are not authorized and were not performed.
  Intercepted Responses tests and unconfigured manual browser paths do not prove
  live provider quality, cost, availability or capability. No real research is
  seeded or imported; synthetic research exists only in disposable databases.
- Private production, production migrations/data/secrets, production sessions,
  customer contact, legal review and physical-device evidence are unavailable.
- The archived `9ec` migration physical-ordinal negative-control databases are
  absent. That exact test is excluded and remains unavailable; 23 other account
  migration tests were exercised separately without inventing the archive.
- Forty-nine broad-suite cases reproduced their failures in a detached,
  unchanged checkout of released `eccc8e901b20ae3cc65a68c9fb2b068a4ceb9375`.
  They are older canonical-graph TRUNCATE fixtures that conflict with released
  immutable field-execution history, ensuing fixture-state cases, and the
  Retell terminal-call isolation timeout. Their exact names are retained in
  `availability-exclusions.json`. They are not passing, silently waived, or
  repaired by widening this equipment package. The available-suite runner
  excludes only those exact baseline names plus the one absent archive control.

Initial broad runs also surfaced Ubuntu distribution-label assertions, pooled
connection cleanup races, and historical status assertions. The writer used
the separately available vanilla PostgreSQL 18.4 binary, reran affected suites,
and updated only obsolete status/asset-upgrade fixture expectations required by
the additive package. Initial failures remain recorded, not rewritten as passes.
