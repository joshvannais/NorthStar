# Unavailable and separate evidence

- Optional Codex Security automation remains unavailable because
  `CODEX_SECURITY_CONFIG_PATH` is unset. It was not requested or retried.
- The 24 `tests/integration/account-migration-010-postgres.test.js` cases need
  four absent `ACCOUNT_MIGRATION_*` disposable URLs. They remain unavailable,
  not passing, and are the only exclusion from the 2,081-test corpus.
- Hosted checks/workflows are absent (`[]`), not passing.
- Browser matrices were not rerun because no UI or browser-consumed byte
  changed. Browser/visual approval remains a separate truth.
- Physical Safari/device evidence remains unavailable; Playwright WebKit is not
  physical Safari.
- Provider calls/accounts/configuration/credentials and live route/map/calendar
  services remain unavailable and were not touched. Part 3 mounted evidence
  retains zero provider calls.
- Railway, production migration, deployed revision, production health, and
  passive acceptance were not authorized for this writer and remain unclaimed.
- No merge, ready-for-review transition, deployment, restart, production access,
  or Mission 22 Part 5 work was performed.
