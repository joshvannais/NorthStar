PR225 F1-F5 correction

F1: explicit shared ECMAScript whitespace set rejects blank evidence without changing recorded bytes or digests.
F2: each known applicability mismatch rejects before remaining unknown fields can produce an incomplete result.
F3: the one invalid legacy separator byte is replaced with the plain word "to"; all other renderer bytes retained.
F4: current company preparation selects exactly matching, currently reviewed rules without substituting legal dates. Calculation independently checks the selected transaction against original legal dates. Expired review remains unavailable; no inferred coverage.
F5: commercial total rows use scoped light/dark ink, preserving Polaris/CAPELLA fonts and treatment.

The unreleased075 helper/evaluator bodies change;72 applied SQL files remain immutable. Runtime policy, privilege and external activation boundaries are unchanged. Registry stays empty/default-off. No Windsor rule, legal conclusion or provider action.

Focused evidence includes22 paired calculation cases,15 paired preparation checks, mounted paid/demo past-transaction and expired-review cases, actual commercial preview/current/history/approval light/dark rendered contrast in Chrome/WebKit and supplemental legacy rendering. Final populated recovery uses new correction-derived policy-only builds; predecessor auditor eae76c36/f29844f5 execution remains separate evidence.
